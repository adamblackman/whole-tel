---
phase: 10-photo-management
plan: 03
type: execute
wave: 2
depends_on: [10-01]
files_modified:
  - src/components/dashboard/DndProvider.tsx
  - src/components/dashboard/SortablePhoto.tsx
  - src/components/dashboard/SectionManager.tsx
  - src/app/(owner)/dashboard/properties/[propertyId]/page.tsx
autonomous: true
requirements: [PHOTO-02, PHOTO-03]
must_haves:
  truths:
    - "Owner can drag photos to reorder them within a section and the order persists"
    - "Owner can create preset sections (Rooms, Common Area, Pool, Exterior) via quick-add"
    - "Owner can create custom-named sections"
    - "Owner can change a photo's section via per-photo dropdown"
    - "Deleting a section moves its photos to General -- photos are never lost"
  artifacts:
    - path: "src/components/dashboard/DndProvider.tsx"
      provides: "use client wrapper for @dnd-kit/react DragDropProvider"
      min_lines: 10
    - path: "src/components/dashboard/SortablePhoto.tsx"
      provides: "Draggable photo card with section dropdown and delete"
      min_lines: 40
    - path: "src/components/dashboard/SectionManager.tsx"
      provides: "Section CRUD UI with preset quick-add and custom input"
      min_lines: 50
  key_links:
    - from: "src/components/dashboard/SortablePhoto.tsx"
      to: "@dnd-kit/react"
      via: "useSortable hook"
      pattern: "useSortable"
    - from: "src/components/dashboard/DndProvider.tsx"
      to: "@dnd-kit/react"
      via: "use client wrapper"
      pattern: "DragDropProvider"
    - from: "src/app/(owner)/dashboard/properties/[propertyId]/page.tsx"
      to: "SectionManager + DndProvider + SortablePhoto"
      via: "component composition in owner property page"
      pattern: "SectionManager|DndProvider|SortablePhoto"
---

<objective>
Add drag-to-reorder via @dnd-kit/react and section management UI to the owner property dashboard, giving owners full control over photo organization and display order.

Purpose: Owners need to arrange photos in meaningful order and organize them into sections for the guest gallery.
Output: DndProvider wrapper, SortablePhoto component, SectionManager component, integrated into owner property page.
</objective>

<execution_context>
@/Users/adamblackman/.claude/get-shit-done/workflows/execute-plan.md
@/Users/adamblackman/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/phases/10-photo-management/10-CONTEXT.md
@.planning/phases/10-photo-management/10-RESEARCH.md
@.planning/phases/10-photo-management/10-01-SUMMARY.md

@src/app/(owner)/dashboard/properties/[propertyId]/page.tsx
@src/lib/actions/photos.ts
@src/types/database.ts
</context>

<tasks>

<task type="auto">
  <name>Task 1: DnD infrastructure and SortablePhoto component</name>
  <files>src/components/dashboard/DndProvider.tsx, src/components/dashboard/SortablePhoto.tsx</files>
  <action>
1. Install @dnd-kit/react and @dnd-kit/helpers:
   ```
   npm install @dnd-kit/react @dnd-kit/helpers
   ```

2. Create `DndProvider.tsx` -- "use client" wrapper for DragDropProvider (Pattern 1 from RESEARCH):
   - Export DndProvider component that wraps children in DragDropProvider
   - Accept onDragEnd callback prop
   - This wrapper exists solely to work around @dnd-kit/react issue #1654

3. Create `SortablePhoto.tsx` -- draggable photo card (Pattern 3 from RESEARCH):
   - "use client" component
   - Uses `useSortable` from `@dnd-kit/react/sortable` with `{ id, index, type: 'photo', group: section ?? 'general' }`
   - Attach `ref` from useSortable to the container div
   - Visual: photo thumbnail (aspect-video), drag handle icon (GripVertical from lucide-react), section dropdown, delete button
   - Drag handle: visible on hover, changes cursor to grab
   - isDragging state: reduce opacity, add ring highlight
   - Section dropdown: lists all available sections (passed as prop), calls updatePhotoSection server action on change
   - Delete button: calls existing deletePhoto server action
   - Use /frontend-design skill: clean card design, smooth hover states, intuitive drag affordance
  </action>
  <verify>
    <automated>npx tsc --noEmit 2>&1 | head -20</automated>
  </verify>
  <done>DndProvider wraps @dnd-kit/react with "use client". SortablePhoto renders draggable photo card with section dropdown and delete. Both compile clean.</done>
</task>

<task type="auto">
  <name>Task 2: SectionManager and owner page integration</name>
  <files>src/components/dashboard/SectionManager.tsx, src/app/(owner)/dashboard/properties/[propertyId]/page.tsx</files>
  <action>
1. Create `SectionManager.tsx` -- "use client" component for section CRUD:
   - Displays current sections as pill/tag buttons (active section highlighted)
   - Clicking a section filters the photo grid to that section and sets it as activeSection for uploads
   - "All" tab shows all photos grouped by section with section headers
   - Preset quick-add buttons: Rooms, Common Area, Pool, Exterior -- only show presets not yet added
   - Custom section input: text field + "Add" button for custom-named sections
   - Delete section button (X on each section pill): calls deleteSection server action, which moves photos to General
   - Section display order: presets first (Rooms, Common Area, Pool, Exterior in that order), then custom sections in creation order
   - Derive section list from the photos data (unique non-null sections) plus any presets
   - Use /frontend-design skill: horizontal scrollable pill bar, clean add-section UX

2. Update owner property detail page to integrate all new components:
   - Import DndProvider, SortablePhoto, SectionManager
   - Wrap the photo grid area in DndProvider
   - Add SectionManager above the photo grid
   - Replace existing photo grid items with SortablePhoto components
   - On DndProvider onDragEnd: use `move` helper from `@dnd-kit/helpers` to compute new order, call reorderPhotos server action with the new ordered array of photo IDs
   - Pass activeSection to PhotoUploader so batch uploads go to the selected section
   - Photos grouped by section when "All" view is active, with section headers between groups
  </action>
  <verify>
    <automated>npx tsc --noEmit 2>&1 | head -20</automated>
  </verify>
  <done>SectionManager provides section CRUD with presets and custom names. Owner property page integrates DnD reorder, section filtering, and section-aware upload. Full photo management workflow functional.</done>
</task>

</tasks>

<verification>
- TypeScript compilation passes
- @dnd-kit/react and @dnd-kit/helpers installed
- DndProvider wraps photo grid with onDragEnd handler
- SortablePhoto uses useSortable hook correctly
- SectionManager shows presets, custom input, delete with photo reassignment
- Owner property page integrates all components
</verification>

<success_criteria>
Owner can drag photos to reorder within sections. Owner can create sections (presets + custom), assign photos via dropdown, and delete sections (photos move to General). The active section determines where new uploads are placed.
</success_criteria>

<output>
After completion, create `.planning/phases/10-photo-management/10-03-SUMMARY.md`
</output>
