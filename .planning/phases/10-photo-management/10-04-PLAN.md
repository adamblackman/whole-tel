---
phase: 10-photo-management
plan: 04
type: execute
wave: 2
depends_on: [10-01]
files_modified:
  - src/components/property/PhotoGallery.tsx
  - src/components/property/SectionedPhotoTour.tsx
  - src/app/(guest)/properties/[propertyId]/page.tsx
autonomous: true
requirements: [PHOTO-05]
must_haves:
  truths:
    - "Guest sees existing 5-photo hero grid pulling from all sections by display_order"
    - "Clicking 'Show all photos' opens a full-screen sectioned photo tour"
    - "Photo tour has section name tabs for quick navigation between sections"
    - "Photo tour supports sequential left/right navigation through all photos"
    - "Gallery has polished, Airbnb-level visual quality"
  artifacts:
    - path: "src/components/property/PhotoGallery.tsx"
      provides: "Extended hero grid that passes section data to tour"
      min_lines: 40
    - path: "src/components/property/SectionedPhotoTour.tsx"
      provides: "Full-screen sectioned gallery with section tabs"
      min_lines: 80
  key_links:
    - from: "src/components/property/PhotoGallery.tsx"
      to: "src/components/property/SectionedPhotoTour.tsx"
      via: "opens tour on 'Show all photos' click"
      pattern: "SectionedPhotoTour|showAll"
    - from: "src/components/property/SectionedPhotoTour.tsx"
      to: "yet-another-react-lightbox"
      via: "lightbox with custom section tabs module"
      pattern: "createModule|Lightbox"
---

<objective>
Build the guest-facing sectioned photo gallery with an Airbnb-quality photo tour featuring section tabs, sequential navigation, and polished visual design.

Purpose: The photo gallery is the primary visual selling point for properties -- it must be beautiful, organized, and easy to navigate.
Output: Extended PhotoGallery with "Show all photos", SectionedPhotoTour with section tabs and lightbox navigation.
</objective>

<execution_context>
@/Users/adamblackman/.claude/get-shit-done/workflows/execute-plan.md
@/Users/adamblackman/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/phases/10-photo-management/10-CONTEXT.md
@.planning/phases/10-photo-management/10-RESEARCH.md
@.planning/phases/10-photo-management/10-01-SUMMARY.md

@src/components/property/PhotoGallery.tsx
@src/app/(guest)/properties/[propertyId]/page.tsx
@src/types/database.ts
</context>

<tasks>

<task type="auto">
  <name>Task 1: SectionedPhotoTour component</name>
  <files>src/components/property/SectionedPhotoTour.tsx</files>
  <action>
Create a new "use client" component for the full-screen sectioned photo tour. Use /frontend-design skill for Airbnb-level polish.

1. Accept props: photos (PropertyPhoto[] with section field), open (boolean), onClose callback
2. Group photos by section for display:
   - Section display order: presets first (Rooms, Common Area, Pool, Exterior), then custom, then General last
   - Within each section, photos ordered by display_order
3. Build slides array for yet-another-react-lightbox from the ordered photos
4. Compute section start indices (which slide index starts each section)
5. Create a custom YARL module using `createModule` (Pattern 4 from RESEARCH):
   - Section tabs bar at the top of the lightbox
   - Use `useLightboxState` to get currentIndex, determine active section
   - Use `useController` goto() to jump to section start index on tab click
   - Tab design (Claude's discretion): horizontal pill bar, active tab has solid background + white text, inactive tabs are ghost style
   - Smooth scroll/transition when jumping between sections
6. Configure YARL with:
   - The custom SectionTabs module
   - Counter plugin (show "3 / 24" style position)
   - Thumbnails plugin for bottom strip (optional polish)
   - Keyboard navigation (left/right arrows)
   - Swipe gestures (touch devices)
7. Close button: top-right corner, clear and accessible
8. Section headers: when scrolling through photos sequentially, show section name as an overlay when entering a new section

Design targets:
- Dark overlay background (YARL default)
- Section tabs: frosted glass or semi-transparent dark bar at top
- Smooth transitions between photos
- Photo counter in bottom-left or top-right
- Responsive: works on mobile (tabs scroll horizontally if needed)
  </action>
  <verify>
    <automated>npx tsc --noEmit 2>&1 | head -20</automated>
  </verify>
  <done>SectionedPhotoTour renders a full-screen lightbox with section tabs, sequential navigation, photo counter, and polished design. Compiles without errors.</done>
</task>

<task type="auto">
  <name>Task 2: Extend PhotoGallery and wire into guest listing page</name>
  <files>src/components/property/PhotoGallery.tsx, src/app/(guest)/properties/[propertyId]/page.tsx</files>
  <action>
1. Extend PhotoGallery.tsx:
   - Keep existing 5-photo hero grid layout (pulls top 5 photos by display_order across all sections)
   - Add "Show all photos" button overlaid on the hero grid (bottom-right corner, semi-transparent background)
   - On click: open SectionedPhotoTour component with all photos
   - Import and render SectionedPhotoTour, controlled by open/onClose state
   - The hero grid itself does NOT show sections -- sections only appear in the full tour
   - Use /frontend-design skill: "Show all photos" button should be subtle but discoverable, Airbnb-style

2. Update guest listing page if needed:
   - Ensure photos are fetched with the section field included
   - Pass full photo data (including section) to PhotoGallery
   - The query should ORDER BY display_order to maintain owner-set ordering

Design: The hero grid stays clean and visual. The "Show all photos" button is the gateway to the organized, sectioned experience.
  </action>
  <verify>
    <automated>npx tsc --noEmit 2>&1 | head -20</automated>
  </verify>
  <done>PhotoGallery has "Show all photos" button that opens SectionedPhotoTour. Guest listing page passes section data to gallery. Full photo tour accessible from property listing.</done>
</task>

</tasks>

<verification>
- TypeScript compilation passes
- PhotoGallery renders hero grid with "Show all photos" button
- SectionedPhotoTour opens with section tabs, sequential navigation
- YARL lightbox configured with custom section tabs module
- Photos ordered by section (presets first) then by display_order within sections
</verification>

<success_criteria>
Guest sees the 5-photo hero grid on property listing. Clicking "Show all photos" opens a full-screen photo tour organized by section with navigable tabs. The tour supports sequential left/right browsing and jumping between sections. Visual quality matches Airbnb photo tour standards.
</success_criteria>

<output>
After completion, create `.planning/phases/10-photo-management/10-04-SUMMARY.md`
</output>
