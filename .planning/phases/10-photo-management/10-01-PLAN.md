---
phase: 10-photo-management
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - supabase/migrations/20260308000003_photo_sections.sql
  - src/types/database.ts
  - src/lib/actions/photos.ts
  - src/components/dashboard/PhotoUploader.tsx
autonomous: true
requirements: [PHOTO-01, PHOTO-03]
must_haves:
  truths:
    - "Owner can select multiple photos at once and they upload sequentially with per-file progress"
    - "Photos are saved with a section value matching the active section at upload time"
    - "Files over 10MB are rejected before upload"
    - "A warning appears when photo count reaches 30"
  artifacts:
    - path: "supabase/migrations/20260308000003_photo_sections.sql"
      provides: "Section column on property_photos"
      contains: "ALTER TABLE property_photos"
    - path: "src/lib/actions/photos.ts"
      provides: "Extended server actions with section support"
      exports: ["savePhotoRecord", "updatePhotoSection", "reorderPhotos", "deleteSection"]
    - path: "src/components/dashboard/PhotoUploader.tsx"
      provides: "Batch upload with progress and section assignment"
      min_lines: 80
  key_links:
    - from: "src/components/dashboard/PhotoUploader.tsx"
      to: "src/lib/actions/photos.ts"
      via: "getSignedUploadUrl + savePhotoRecord calls in sequential loop"
      pattern: "savePhotoRecord.*section"
---

<objective>
Add section column to property_photos, extend server actions for section/reorder support, and upgrade PhotoUploader to batch upload with inline progress and section assignment.

Purpose: Foundation for all photo management features -- the DB schema, server actions, and batch upload UX that everything else builds on.
Output: Migration file, extended types, extended server actions, upgraded PhotoUploader with multi-file + progress + section support.
</objective>

<execution_context>
@/Users/adamblackman/.claude/get-shit-done/workflows/execute-plan.md
@/Users/adamblackman/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/10-photo-management/10-CONTEXT.md
@.planning/phases/10-photo-management/10-RESEARCH.md

@src/types/database.ts
@src/lib/actions/photos.ts
@src/components/dashboard/PhotoUploader.tsx
@src/app/(owner)/dashboard/properties/[propertyId]/page.tsx
</context>

<tasks>

<task type="auto">
  <name>Task 1: Migration, types, and server actions</name>
  <files>supabase/migrations/20260308000003_photo_sections.sql, src/types/database.ts, src/lib/actions/photos.ts</files>
  <action>
1. Create migration `20260308000003_photo_sections.sql`:
   - ALTER TABLE property_photos ADD COLUMN section text (nullable, null = General)
   - CREATE INDEX idx_property_photos_section ON property_photos (property_id, section, display_order)

2. Update `PropertyPhoto` type in database.ts: add `section: string | null` field.

3. Extend `src/lib/actions/photos.ts` with these changes:
   - Update `savePhotoRecord` to accept optional `section` parameter (string | null). Pass it to the insert. Default null.
   - Add `updatePhotoSection(photoId, propertyId, section)` -- updates section for a single photo, revalidates both dashboard and guest paths.
   - Add `reorderPhotos(propertyId, photoIds: string[])` -- takes an ordered array of photo IDs, updates display_order to match array index (0, 1, 2...). Revalidates both paths.
   - Add `deleteSection(propertyId, sectionName)` -- sets section=null for all photos with that section in the property, then revalidates. Photos are never lost.

Follow the exact patterns from 10-RESEARCH.md code examples. Every mutation must call revalidatePath for both `/dashboard/properties/${propertyId}` and `/properties/${propertyId}`.
  </action>
  <verify>
    <automated>npx tsc --noEmit 2>&1 | head -20</automated>
  </verify>
  <done>Migration file exists with section column + index. PropertyPhoto type has section field. Four server actions (savePhotoRecord updated, updatePhotoSection, reorderPhotos, deleteSection) compile without errors.</done>
</task>

<task type="auto">
  <name>Task 2: Batch upload PhotoUploader with progress and section support</name>
  <files>src/components/dashboard/PhotoUploader.tsx</files>
  <action>
Overhaul PhotoUploader.tsx to support batch upload with section assignment. Use /frontend-design skill for polished upload UX.

Key changes:
1. Change file input to accept `multiple`. Keep the existing "Upload Photo" button but make it accept multiple files.
2. Add 10MB file size validation -- reject files over 10MB with an error toast before upload starts.
3. Add soft warning when property has 30+ photos (show a yellow warning banner, don't block).
4. Implement sequential upload loop using the pattern from RESEARCH.md Pattern 2:
   - Track upload queue state: `UploadItem[]` with `{ file, status: 'pending'|'uploading'|'done'|'error', error? }`
   - Show inline progress list below the upload button during batch upload
   - Each file shows: filename (truncated), status icon (spinner/check/X), error message if failed
   - Photos appear in the grid as each completes (existing grid re-renders from server data via revalidation)
   - Use functional updater `setUploadQueue(prev => ...)` to avoid stale state (Pitfall 3 from RESEARCH)
5. Accept `activeSection` prop (string | null) -- pass to savePhotoRecord for each uploaded photo.
6. The component should be "use client" (it already is).

Design polish (via /frontend-design):
- Progress list: subtle card with rounded corners, each item is a row with file icon, truncated name, and animated status indicator
- Upload button: maintain existing style but add a subtle file count badge when multiple files selected
- Smooth transitions as items complete (opacity fade on done items after brief delay)
  </action>
  <verify>
    <automated>npx tsc --noEmit 2>&1 | head -20</automated>
  </verify>
  <done>PhotoUploader accepts multiple files, shows per-file upload progress, validates 10MB limit, warns at 30 photos, assigns active section to each uploaded photo. TypeScript compiles clean.</done>
</task>

</tasks>

<verification>
- TypeScript compilation passes with no errors
- Migration SQL is valid (ALTER TABLE + CREATE INDEX)
- PhotoUploader accepts `multiple` and `activeSection` props
- Server actions include section parameter and revalidate both paths
</verification>

<success_criteria>
Owner can select multiple photos, see them upload one by one with progress indicators, and each photo is saved with the active section value. Files over 10MB are rejected. The migration adds the section column to property_photos.
</success_criteria>

<output>
After completion, create `.planning/phases/10-photo-management/10-01-SUMMARY.md`
</output>
