---
phase: 10-photo-management
plan: 02
type: execute
wave: 1
depends_on: []
files_modified:
  - src/lib/actions/photos.ts
  - src/components/dashboard/AddOnForm.tsx
  - src/components/property/AddOnCard.tsx
autonomous: true
requirements: [PHOTO-04]
must_haves:
  truths:
    - "Owner can upload a photo for an individual experience/add-on"
    - "Experience card displays the photo as a hero image above name and pricing"
    - "If no photo is uploaded, the experience card falls back to text-only style"
  artifacts:
    - path: "src/lib/actions/photos.ts"
      provides: "Experience photo upload server actions"
      exports: ["getExperienceUploadUrl", "saveExperiencePhoto"]
    - path: "src/components/dashboard/AddOnForm.tsx"
      provides: "Photo upload button in experience form"
      min_lines: 50
    - path: "src/components/property/AddOnCard.tsx"
      provides: "Hero image display on experience card"
      min_lines: 30
  key_links:
    - from: "src/components/dashboard/AddOnForm.tsx"
      to: "src/lib/actions/photos.ts"
      via: "getExperienceUploadUrl + saveExperiencePhoto"
      pattern: "getExperienceUploadUrl|saveExperiencePhoto"
    - from: "src/components/property/AddOnCard.tsx"
      to: "photo_url"
      via: "conditional rendering of hero image"
      pattern: "photo_url.*Image|img"
---

<objective>
Add photo upload to experience/add-on forms and display experience photos as hero images on guest-facing add-on cards.

Purpose: Experiences with photos feel premium and worth selecting -- this makes add-ons visually compelling.
Output: Experience photo upload in AddOnForm, hero image display in AddOnCard, supporting server actions.
</objective>

<execution_context>
@/Users/adamblackman/.claude/get-shit-done/workflows/execute-plan.md
@/Users/adamblackman/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/phases/10-photo-management/10-CONTEXT.md
@.planning/phases/10-photo-management/10-RESEARCH.md

@src/lib/actions/photos.ts
@src/components/dashboard/AddOnForm.tsx
@src/components/property/AddOnCard.tsx
@src/types/database.ts
</context>

<tasks>

<task type="auto">
  <name>Task 1: Experience photo server actions and AddOnForm upload</name>
  <files>src/lib/actions/photos.ts, src/components/dashboard/AddOnForm.tsx</files>
  <action>
1. Add two server actions to `src/lib/actions/photos.ts` (following exact patterns from RESEARCH.md "Experience Photo Upload" section):
   - `getExperienceUploadUrl(addOnId, propertyId)` -- verify add-on belongs to owner's property, generate signed URL with path `experiences/${userId}/${addOnId}/${uuid}`
   - `saveExperiencePhoto(addOnId, storagePath)` -- construct public URL from storage path, update add_ons.photo_url

   NOTE: This file is also modified by Plan 01. Since both plans are Wave 1, the executor must handle merge. The experience actions are purely additive (new exports), no conflicts with Plan 01's section-related changes.

2. Add photo upload to AddOnForm.tsx:
   - Add a photo upload section below the existing form fields
   - Show current photo thumbnail if `photo_url` exists on the add-on
   - "Upload Photo" button triggers hidden file input (accept="image/*", single file)
   - On file select: validate 10MB limit, get signed URL via getExperienceUploadUrl, upload to Supabase Storage, save via saveExperiencePhoto
   - Show uploading spinner during upload
   - After upload: show the new photo thumbnail with a "Remove" option (sets photo_url to null)
   - Use /frontend-design skill: clean upload area with dashed border, thumbnail preview with overlay remove button

3. Use storage bucket "property-photos" but with the `experiences/` path prefix to avoid collisions (Pitfall 6 from RESEARCH).
  </action>
  <verify>
    <automated>npx tsc --noEmit 2>&1 | head -20</automated>
  </verify>
  <done>AddOnForm has photo upload button, displays thumbnail preview, handles upload via signed URL to experiences/ path prefix. Server actions compile and export correctly.</done>
</task>

<task type="auto">
  <name>Task 2: AddOnCard hero image display</name>
  <files>src/components/property/AddOnCard.tsx</files>
  <action>
Update AddOnCard.tsx to display experience photo as hero image. Use /frontend-design skill for polished card design.

1. Check if `photo_url` exists on the add-on data
2. If photo exists: render next/image as hero image above the card content (name, description, pricing)
   - Use aspect-video ratio for consistent sizing
   - Rounded top corners (card style), object-cover
   - Card content sits below the image with proper padding
3. If no photo: fall back to current text-only card style (no layout shift, no empty image placeholder)
4. The card should look premium -- subtle shadow, clean typography, the photo makes it feel like a real product listing

Design: Think Airbnb experience cards -- photo dominates the top, compact info below with price prominent.
  </action>
  <verify>
    <automated>npx tsc --noEmit 2>&1 | head -20</automated>
  </verify>
  <done>AddOnCard conditionally renders hero image when photo_url exists. Falls back gracefully to text-only. Card has polished, premium appearance.</done>
</task>

</tasks>

<verification>
- TypeScript compilation passes
- AddOnForm includes file upload UI with signed URL pattern
- AddOnCard conditionally renders hero image
- Experience photos use `experiences/` storage path prefix
</verification>

<success_criteria>
Owner can upload a photo to an experience via AddOnForm. The photo appears as a hero image on the guest-facing AddOnCard. Experiences without photos display as text-only cards without visual breakage.
</success_criteria>

<output>
After completion, create `.planning/phases/10-photo-management/10-02-SUMMARY.md`
</output>
