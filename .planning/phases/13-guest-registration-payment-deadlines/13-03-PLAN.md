---
phase: 13-guest-registration-payment-deadlines
plan: 03
type: execute
wave: 2
depends_on:
  - 13-01
files_modified:
  - src/components/booking/PaymentDeadlineCountdown.tsx
  - src/components/booking/ManualAttendeeForm.tsx
  - src/components/booking/BookingDetails.tsx
  - src/components/booking/GuestList.tsx
  - src/app/api/cron/expire-bookings/route.ts
  - vercel.json
  - src/app/bookings/invitations/[token]/page.tsx
autonomous: true
requirements:
  - PAY-01
  - PAY-02
  - PAY-05
  - PAY-06
  - PAY-08

must_haves:
  truths:
    - "Booking detail page shows payment deadline countdown for pending bookings with Complete Payment CTA"
    - "Booking detail page shows activity booking deadline date"
    - "Guest list displays full_name and phone for accepted attendees"
    - "Group lead can add attendees manually via inline form in guest list"
    - "Invitation acceptance page collects full_name and phone before completing"
    - "Cron endpoint expires pending bookings past payment_deadline every 15 minutes"
    - "Expired bookings display with 'expired' status badge"
  artifacts:
    - path: "src/components/booking/PaymentDeadlineCountdown.tsx"
      provides: "Client-side countdown timer component"
      min_lines: 25
    - path: "src/components/booking/ManualAttendeeForm.tsx"
      provides: "Inline form for manual attendee entry"
      min_lines: 30
    - path: "src/components/booking/BookingDetails.tsx"
      provides: "Extended booking details with deadline display and expired state"
    - path: "src/components/booking/GuestList.tsx"
      provides: "Extended guest list showing full_name, phone"
    - path: "src/app/api/cron/expire-bookings/route.ts"
      provides: "Vercel Cron handler for booking expiry"
      exports: ["GET"]
    - path: "vercel.json"
      provides: "Cron schedule configuration"
      contains: "expire-bookings"
  key_links:
    - from: "src/components/booking/BookingDetails.tsx"
      to: "src/components/booking/PaymentDeadlineCountdown.tsx"
      via: "Component import and render"
      pattern: "PaymentDeadlineCountdown"
    - from: "src/app/api/cron/expire-bookings/route.ts"
      to: "bookings table"
      via: "createAdminClient update query"
      pattern: "status.*expired.*payment_deadline"
    - from: "src/components/booking/BookingDetails.tsx"
      to: "src/components/booking/ManualAttendeeForm.tsx"
      via: "Component import for manual attendee entry"
      pattern: "ManualAttendeeForm"
---

<objective>
Build UI components for deadline display, registration form on invitation acceptance, manual attendee entry, and the Vercel Cron route for automated booking expiry.

Purpose: These are the user-facing and automation pieces that make deadlines visible (PAY-05, PAY-06), enforce registration (PAY-01), enable manual attendee addition (PAY-02), and auto-expire unpaid bookings (PAY-08).
Output: Complete UI for deadline/registration and working cron endpoint.
</objective>

<execution_context>
@/Users/adamblackman/.claude/get-shit-done/workflows/execute-plan.md
@/Users/adamblackman/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/13-guest-registration-payment-deadlines/13-CONTEXT.md
@.planning/phases/13-guest-registration-payment-deadlines/13-RESEARCH.md
@.planning/phases/13-guest-registration-payment-deadlines/13-01-SUMMARY.md

<interfaces>
<!-- Types from Plan 01 -->

From src/types/database.ts (after Plan 01):
```typescript
export type BookingStatus = 'pending' | 'confirmed' | 'cancelled' | 'expired'

export interface Booking {
  // ... existing ...
  payment_deadline: string | null
  activity_deadline: string | null
  stripe_checkout_url: string | null
}

export interface BookingInvitation {
  // ... existing ...
  full_name: string | null
  phone: string | null
}
```

<!-- Action signatures from Plan 02 -->
From src/lib/actions/booking-invitations.ts (after Plan 02):
```typescript
export async function acceptInvitation(
  token: string,
  registration: { fullName: string; phone: string }
): Promise<{ success: boolean; bookingId?: string; error?: string }>

export async function addAttendeeManually(input: {
  bookingId: string; fullName: string; email: string; phone: string
}): Promise<{ success: boolean; error?: string }>
```

<!-- Existing component interfaces -->

From src/components/booking/BookingDetails.tsx:
```typescript
interface BookingDetailsProps {
  bookingId: string
  checkIn: string
  checkOut: string
  guestCount: number
  subtotal: number
  addOnsTotal: number
  processingFee: number
  total: number
  status: string
  maxGuests: number
  bookingAddOns: BookingAddOnRow[]
  invitations: InvitationRow[]
}
```

From src/components/booking/GuestList.tsx:
```typescript
interface GuestListProps {
  invitations: Array<{
    id: string
    email: string
    status: InvitationStatus
    created_at: string
  }>
}
```

From src/lib/supabase/admin.ts:
```typescript
export function createAdminClient(): SupabaseClient
```
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Create PaymentDeadlineCountdown, ManualAttendeeForm, and update invitation acceptance page</name>
  <files>src/components/booking/PaymentDeadlineCountdown.tsx, src/components/booking/ManualAttendeeForm.tsx, src/app/bookings/invitations/[token]/page.tsx</files>
  <action>
**PaymentDeadlineCountdown.tsx** — New 'use client' component:
- Props: `{ deadline: string }` (ISO timestamp)
- Uses `useState` + `useEffect` with `setInterval(1000)` to update countdown every second
- Computes remaining seconds: `Math.max(0, Math.floor((new Date(deadline).getTime() - Date.now()) / 1000))`
- Displays as `{hours}h {minutes}m {seconds}s remaining` with `text-amber-600 text-sm font-medium tabular-nums`
- When remaining <= 0, show `"Payment window expired"` in `text-destructive text-sm`
- Cleanup interval on unmount
- Use Clock icon from lucide-react next to the countdown

**ManualAttendeeForm.tsx** — New 'use client' component:
- Props: `{ bookingId: string; onClose: () => void }`
- Three input fields: Full Name, Email, Phone — using shadcn/ui Input component
- Submit button calls `addAttendeeManually` server action (import from `@/lib/actions/booking-invitations`)
- Show loading state on submit, error message on failure (use `useTransition` or `useState`)
- On success, call `onClose()` to collapse the form
- Keep it inline-friendly (not a modal) — render inside the guest list section
- Validate client-side: all three fields required, email format check

**Invitation acceptance page** (`src/app/bookings/invitations/[token]/page.tsx`):
- Find the existing invitation acceptance page. It currently calls `acceptInvitation(token)` on button click.
- Add an intermediate state: when user clicks "Accept", show a form collecting `fullName` and `phone` (using shadcn/ui Input).
- Only call `acceptInvitation(token, { fullName, phone })` after both fields are filled and submitted.
- Use the same amber-themed styling as the rest of the booking flow.
- If the page is a Server Component with a client child for actions, the form must be in the client child.
  </action>
  <verify>
    <automated>cd /Users/adamblackman/Code/whole-tel && npx tsc --noEmit 2>&1 | head -20</automated>
  </verify>
  <done>PaymentDeadlineCountdown renders a live countdown with amber styling. ManualAttendeeForm provides inline entry for name/email/phone. Invitation acceptance page gates on name+phone before calling acceptInvitation.</done>
</task>

<task type="auto">
  <name>Task 2: Extend BookingDetails and GuestList with deadline display and registration data</name>
  <files>src/components/booking/BookingDetails.tsx, src/components/booking/GuestList.tsx</files>
  <action>
**BookingDetails.tsx changes:**

1. Extend `BookingDetailsProps` with new fields:
   - `paymentDeadline: string | null`
   - `activityDeadline: string | null`
   - `stripeCheckoutUrl: string | null`

2. Add deadline section to the JSX (after the guest count section, before price breakdown):
   - If `status === 'pending'` and `paymentDeadline` is set:
     - Render `<PaymentDeadlineCountdown deadline={paymentDeadline} />` (import from `./PaymentDeadlineCountdown`)
     - Next to it, if `stripeCheckoutUrl` is set, render a "Complete Payment" link (`<a>` tag or shadcn Button with `asChild`) styled as a small CTA button linking to `stripeCheckoutUrl`
     - Use `Clock` icon from lucide-react
   - If `activityDeadline` is set (regardless of status):
     - Show "Activity booking deadline: {formatted date}" with Calendar icon from lucide-react
     - Format date as readable string (e.g., "Mar 15, 2026")

3. Handle expired status:
   - If `status === 'expired'`, show an amber/yellow banner: "This booking has expired. The payment deadline has passed."
   - Do not show the guest editor, invite form, or payment CTA for expired bookings
   - The price breakdown should still be visible (read-only context)

4. Extend `InvitationRow` interface with `full_name: string | null` and `phone: string | null`

5. Add ManualAttendeeForm to the guest management section:
   - Below the existing InviteGuestForm, add a "Add manually" button (for confirmed bookings only)
   - When clicked, show `<ManualAttendeeForm bookingId={bookingId} onClose={...} />`

6. Update the parent page that renders BookingDetails to pass the new props. Find it by searching for `<BookingDetails` usage — it should be fetching from the bookings table. Add `payment_deadline`, `activity_deadline`, `stripe_checkout_url` to the select query.

**GuestList.tsx changes:**

1. Extend the `GuestListProps` invitations array type to include `full_name: string | null` and `phone: string | null`

2. For accepted invitations, display `full_name` (bold) and `phone` below the email
   - Layout: name on one line, email + phone on the next line (both in muted text)
   - For pending/declined invitations that have no name/phone, show email only (existing behavior)

3. Add an expired badge variant in the `StatusBadge` function if booking-level status is needed, or at minimum handle the case where the parent passes booking status context.

Note: Per CONTEXT.md, styling should feel "urgent but not alarming — amber/yellow, not red" for payment deadlines.
  </action>
  <verify>
    <automated>cd /Users/adamblackman/Code/whole-tel && npx tsc --noEmit 2>&1 | head -20</automated>
  </verify>
  <done>BookingDetails shows payment countdown with CTA for pending bookings, activity deadline for all bookings, and handles expired state. GuestList displays registration data (name, phone) for accepted invitations. ManualAttendeeForm is accessible from the guest management section.</done>
</task>

<task type="auto">
  <name>Task 3: Create Vercel Cron route for booking expiry and vercel.json</name>
  <files>src/app/api/cron/expire-bookings/route.ts, vercel.json</files>
  <action>
**Cron route** (`src/app/api/cron/expire-bookings/route.ts`):

Create a GET route handler:
1. Verify `authorization` header equals `Bearer ${process.env.CRON_SECRET}`. Return 401 if not.
2. Use `createAdminClient()` (import from `@/lib/supabase/admin`) — the cron runs without user context.
3. Query: update all bookings where `status = 'pending'` AND `payment_deadline < now()`, setting `status = 'expired'` and `updated_at = new Date().toISOString()`. Select `id` to count affected rows.
4. Log the count: `console.log('expire-bookings: expired ${data?.length ?? 0} bookings')`
5. Return `NextResponse.json({ expired: data?.length ?? 0 })` on success.
6. On error, log and return 500 with error message.

Import `NextResponse` from `next/server`.

**vercel.json** (new file at project root):
```json
{
  "crons": [
    {
      "path": "/api/cron/expire-bookings",
      "schedule": "*/15 * * * *"
    }
  ]
}
```

Note from research: Vercel Hobby plan restricts crons to daily. If on Hobby, this will run daily instead of every 15 min. The schedule is correct for Pro plan. The route itself is idempotent — missed ticks are self-healing.

Important: The `CRON_SECRET` env var must be set in Vercel project settings. This is a `user_setup` item but does not block development — the route works locally when tested with the correct header.
  </action>
  <verify>
    <automated>cd /Users/adamblackman/Code/whole-tel && npx tsc --noEmit 2>&1 | head -20 && test -f vercel.json && echo "PASS: vercel.json exists" && test -f src/app/api/cron/expire-bookings/route.ts && echo "PASS: cron route exists"</automated>
  </verify>
  <done>Cron route authenticates via CRON_SECRET, expires pending bookings past payment_deadline, returns count. vercel.json configures 15-minute schedule. Both files compile cleanly.</done>
</task>

</tasks>

<verification>
- All new components compile: `npx tsc --noEmit`
- PaymentDeadlineCountdown is a 'use client' component with interval-based countdown
- BookingDetails passes new deadline props and renders them appropriately
- GuestList shows registration data for accepted invitations
- Cron route returns 401 without proper auth header
- vercel.json has correct cron configuration
- Invitation acceptance page gates on name + phone form
</verification>

<success_criteria>
- Payment deadline countdown visible on pending booking detail page (PAY-05)
- Activity deadline date displayed on booking detail page (PAY-06)
- Full name and phone shown in guest list for accepted invitations (PAY-01)
- Manual attendee form accessible and functional in guest list section (PAY-02)
- Invitation acceptance requires name and phone before completing (PAY-01)
- Cron route expires overdue pending bookings (PAY-08)
- Expired bookings show appropriate status UI (PAY-08)
</success_criteria>

<output>
After completion, create `.planning/phases/13-guest-registration-payment-deadlines/13-03-SUMMARY.md`
</output>
