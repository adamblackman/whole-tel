---
phase: 07-landing-page-and-polish
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - src/app/page.tsx
  - src/components/landing/Hero.tsx
  - src/components/landing/BrandStory.tsx
  - src/components/landing/FeaturedProperties.tsx
  - src/components/landing/DestinationCards.tsx
  - src/components/landing/Testimonials.tsx
autonomous: true
requirements:
  - PAGE-01

must_haves:
  truths:
    - "Homepage displays a full-width hero with brand name, tagline, and CTA buttons (Browse Villas, Get Started)"
    - "Homepage shows a brand story section explaining the Whole-Tel concept"
    - "Homepage displays featured properties from the database using PropertyListingCard"
    - "Homepage shows destination cards linking to /properties?destination=X for each destination"
    - "Homepage displays static testimonials section"
    - "Homepage has its own transparent hero nav (not GuestNav — it is outside the (guest) route group)"
  artifacts:
    - path: "src/app/page.tsx"
      provides: "Landing page Server Component composing all sections"
      contains: "FeaturedProperties"
    - path: "src/components/landing/Hero.tsx"
      provides: "Hero section with transparent nav overlay, gradient background, animated text"
    - path: "src/components/landing/FeaturedProperties.tsx"
      provides: "Featured properties grid reusing PropertyListingCard"
      contains: "PropertyListingCard"
    - path: "src/components/landing/DestinationCards.tsx"
      provides: "Destination browsing cards with gradient overlays"
      contains: "properties?destination="
  key_links:
    - from: "src/app/page.tsx"
      to: "supabase.from('properties')"
      via: "Server Component data fetch"
      pattern: "from\\('properties'\\)"
    - from: "src/components/landing/FeaturedProperties.tsx"
      to: "src/components/browse/PropertyListingCard.tsx"
      via: "import and render"
      pattern: "PropertyListingCard"
    - from: "src/components/landing/DestinationCards.tsx"
      to: "/properties?destination="
      via: "Link href"
      pattern: "destination="
---

<objective>
Rewrite the homepage into a compelling marketing landing page with hero section, brand story, featured properties from the database, destination browsing cards, and testimonials.

Purpose: Convert visitors into bookers with a polished first impression that communicates the tropical party villa brand.
Output: Full landing page at `/` with 5 composed sections.
</objective>

<execution_context>
@/Users/adamblackman/.claude/get-shit-done/workflows/execute-plan.md
@/Users/adamblackman/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/07-landing-page-and-polish/07-RESEARCH.md

@src/app/page.tsx
@src/components/browse/PropertyListingCard.tsx
@src/app/globals.css
</context>

<interfaces>
<!-- Existing components and patterns the executor needs -->

From src/components/browse/PropertyListingCard.tsx:
```typescript
interface PropertyListingCardProps {
  property: {
    id: string
    name: string
    location: string
    bedrooms: number
    bathrooms: number
    max_guests: number
    nightly_rate: number
    property_photos: Array<{ id: string; storage_path: string; display_order: number }>
  }
}
export function PropertyListingCard({ property }: PropertyListingCardProps)
```

Brand palette (from globals.css :root):
```css
--brand-amber: oklch(0.78 0.15 75);   /* Warm accent — CTAs, highlights */
--brand-teal: oklch(0.65 0.14 185);   /* Primary brand — nav, headings */
--brand-sand: oklch(0.92 0.04 85);    /* Light background sections */
--brand-palm: oklch(0.42 0.12 150);   /* Dark accent — footer, contrast text */
```
Tailwind usage: `bg-brand-teal`, `text-brand-amber`, `bg-brand-sand`, `text-brand-palm`

Entrance animations (tw-animate-css, already installed):
```
animate-in fade-in duration-700
animate-in slide-in-from-bottom-4 duration-500 delay-200
```
</interfaces>

<tasks>

<task type="auto">
  <name>Task 1: Create landing page section components</name>
  <files>
    src/components/landing/Hero.tsx
    src/components/landing/BrandStory.tsx
    src/components/landing/FeaturedProperties.tsx
    src/components/landing/DestinationCards.tsx
    src/components/landing/Testimonials.tsx
  </files>
  <action>
Create 5 section components in `src/components/landing/`:

**Hero.tsx** — Client Component (`'use client'` for tw-animate-css entrance animations):
- Full-width gradient background: `bg-gradient-to-br from-brand-teal via-cyan-500 to-emerald-400`
- Transparent nav overlay at top: Whole-Tel logo (left), "Browse Villas" ghost button + "Get Started" amber CTA button (right) linking to `/properties` and `/login` respectively. On mobile, keep both buttons but smaller.
- Centered content: large heading "Your Next Party Starts Here" with `animate-in fade-in duration-700`, subheading "Luxury party villas with curated local experiences in Cabo, Puerto Vallarta, and Miami" with `animate-in fade-in duration-700 delay-300`, and a "Browse Villas" amber CTA button below with `animate-in slide-in-from-bottom-4 duration-500 delay-500`
- Min height: `min-h-[70vh]` with flex centering
- Use Button from shadcn/ui, Link from next/link

**BrandStory.tsx** — Server Component:
- `bg-brand-sand` background section
- Two-column layout on desktop (text left, visual right), stacked on mobile
- Heading: "The Whole Experience" in `text-brand-palm`
- Copy: "Whole-Tel isn't just a place to stay — it's the whole trip. We pair stunning party villas with handpicked local experiences: private chefs, boat excursions, VIP nightlife, and more. One booking, one crew, one unforgettable trip."
- Right column: a decorative card or icon grid showing 3-4 experience types (Chef hat, Boat/Anchor, Music/Party, Sunset icons from lucide-react) with short labels

**FeaturedProperties.tsx** — Server Component:
- Accepts `properties` prop (same shape as PropertyListingCard expects, with `property_photos` including `id` field)
- Heading: "Featured Villas" with subheading "Handpicked party-ready properties"
- Renders up to 3 properties in a responsive grid using `PropertyListingCard` from `@/components/browse/PropertyListingCard`
- If no properties, show a friendly empty state
- Section padding: `py-16 px-6`, max-w-7xl centered

**DestinationCards.tsx** — Server Component:
- Heading: "Choose Your Destination"
- 3 destination cards in a grid: Cabo San Lucas, Puerto Vallarta, Miami
- Each card: rounded-2xl, min-h-[200px], gradient overlay (dark at bottom for text legibility), destination name in white bold text, short tagline underneath
- Use distinct gradient backgrounds for each card since we don't have real destination photos yet (e.g., teal-to-cyan for Cabo, amber-to-orange for PV, purple-to-blue for Miami)
- Each card is a Link to `/properties?destination={encodeURIComponent(destinationName)}`
- Hover effect: scale-105 transition

**Testimonials.tsx** — Server Component:
- `bg-brand-sand` background
- Heading: "What Our Guests Say"
- 3 static testimonial cards with placeholder quotes, names, and trip destinations. Use Card from shadcn/ui.
- Example quotes about the villa experience, add-on experiences, and group trip coordination
- Each card: quote text, reviewer name, destination visited
  </action>
  <verify>
    <automated>cd /Users/adamblackman/Code/whole-tel && ls src/components/landing/Hero.tsx src/components/landing/BrandStory.tsx src/components/landing/FeaturedProperties.tsx src/components/landing/DestinationCards.tsx src/components/landing/Testimonials.tsx && npx next build 2>&1 | tail -20</automated>
  </verify>
  <done>All 5 section components exist, export correctly, and the project builds without errors</done>
</task>

<task type="auto">
  <name>Task 2: Rewrite homepage to compose landing sections</name>
  <files>
    src/app/page.tsx
  </files>
  <action>
Rewrite `src/app/page.tsx` as a Server Component that:

1. Fetches properties from Supabase: `supabase.from('properties').select('*, property_photos(id, storage_path, display_order)').order('nightly_rate', { ascending: true })` — note the `id` in property_photos select (required by PropertyListingCard interface)
2. Composes sections in order: Hero, BrandStory, FeaturedProperties (pass properties), DestinationCards, Testimonials
3. Adds a simple footer at the bottom: Whole-Tel brand name, copyright year, links to About, Contact, Browse Villas. `bg-brand-palm text-white` styling.

The page is NOT in the `(guest)` route group — it has its own Hero nav. Do NOT import GuestNav.

Structure:
```tsx
export default async function HomePage() {
  const supabase = await createClient()
  const { data: properties } = await supabase...

  return (
    <div className="min-h-screen">
      <Hero />
      <BrandStory />
      <FeaturedProperties properties={properties ?? []} />
      <DestinationCards />
      <Testimonials />
      <footer>...</footer>
    </div>
  )
}
```
  </action>
  <verify>
    <automated>cd /Users/adamblackman/Code/whole-tel && npx next build 2>&1 | tail -20</automated>
  </verify>
  <done>Homepage at `/` renders hero with nav, brand story, featured property cards from database, destination cards linking to browse page, testimonials, and footer. Build succeeds.</done>
</task>

</tasks>

<verification>
- `npx next build` succeeds without errors
- Homepage (`/`) renders all 5 sections plus footer
- FeaturedProperties uses PropertyListingCard (not duplicated card logic)
- Destination cards link to `/properties?destination=X` with correct encoding
- Hero nav links to `/properties` and `/login`
- No `'use client'` on Server Components (only Hero needs it for animations)
</verification>

<success_criteria>
The homepage at `/` displays a polished marketing landing page with hero section (gradient + animated text + CTAs), brand story, featured properties from the database, destination browsing cards, and testimonials — all with the tropical chill brand aesthetic.
</success_criteria>

<output>
After completion, create `.planning/phases/07-landing-page-and-polish/07-01-SUMMARY.md`
</output>
