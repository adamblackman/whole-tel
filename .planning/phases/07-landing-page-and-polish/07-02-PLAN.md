---
phase: 07-landing-page-and-polish
plan: 02
type: execute
wave: 1
depends_on: []
files_modified:
  - src/app/(guest)/about/page.tsx
  - src/app/(guest)/contact/page.tsx
  - src/components/contact/ContactForm.tsx
  - src/lib/actions/contact.ts
autonomous: true
requirements:
  - PAGE-02
  - PAGE-03

must_haves:
  truths:
    - "An About Us page at /about explains the Whole-Tel brand story"
    - "A Contact page at /contact has a form with name, email, and message fields"
    - "Submitting the contact form sends an email to adam@whole-tel.com via Resend"
    - "Both pages inherit GuestNav from the (guest) layout"
    - "Contact form shows success/error feedback after submission"
  artifacts:
    - path: "src/app/(guest)/about/page.tsx"
      provides: "About Us page with brand story content"
      contains: "Whole-Tel"
    - path: "src/app/(guest)/contact/page.tsx"
      provides: "Contact page shell importing ContactForm"
      contains: "ContactForm"
    - path: "src/components/contact/ContactForm.tsx"
      provides: "Client Component contact form with useActionState"
      contains: "use client"
    - path: "src/lib/actions/contact.ts"
      provides: "Server Action that sends email via Resend"
      contains: "use server"
  key_links:
    - from: "src/components/contact/ContactForm.tsx"
      to: "src/lib/actions/contact.ts"
      via: "useActionState(sendContactEmail)"
      pattern: "sendContactEmail"
    - from: "src/lib/actions/contact.ts"
      to: "src/lib/email.ts"
      via: "getResend() import"
      pattern: "getResend"
    - from: "src/app/(guest)/about/page.tsx"
      to: "src/app/(guest)/layout.tsx"
      via: "route group inheritance"
      pattern: "GuestNav"
---

<objective>
Create the About Us and Contact pages that complete the marketing page set for Whole-Tel.

Purpose: Give visitors context about the brand and a way to reach out with inquiries.
Output: `/about` page with brand story, `/contact` page with working email form.
</objective>

<execution_context>
@/Users/adamblackman/.claude/get-shit-done/workflows/execute-plan.md
@/Users/adamblackman/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/07-landing-page-and-polish/07-RESEARCH.md

@src/app/(guest)/layout.tsx
@src/lib/email.ts
</context>

<interfaces>
<!-- Existing patterns the executor needs -->

From src/lib/email.ts:
```typescript
export function getResend(): Resend
```

From src/app/(guest)/layout.tsx:
```typescript
// Pages in (guest)/ route group automatically get GuestNav
// URL is /about and /contact (not /guest/about)
```

useActionState pattern (React 19, used throughout project):
```typescript
const [state, formAction, isPending] = useActionState(serverAction, null)
```

Zod v4 note: Use `.issues` not `.errors` on ZodError.
</interfaces>

<tasks>

<task type="auto">
  <name>Task 1: Create contact Server Action and ContactForm component</name>
  <files>
    src/lib/actions/contact.ts
    src/components/contact/ContactForm.tsx
  </files>
  <action>
**src/lib/actions/contact.ts** — Server Action:
- `'use server'` directive at top
- Import `z` from `zod`, `getResend` from `@/lib/email`
- Define `ContactSchema` with: `name` (string, min 1, "Name is required"), `email` (string, email, "Valid email required"), `message` (string, min 10, "Message must be at least 10 characters")
- Export `sendContactEmail` function accepting `(_prevState: unknown, formData: FormData)` — matches useActionState signature
- Parse formData fields through ContactSchema with safeParse
- On validation failure: return `{ error: parsed.error.issues[0].message }` (Zod v4 uses `.issues`)
- On success: call `getResend().emails.send()` with:
  - `from`: Use the same from address pattern as the booking confirmation email in the project. Check what Phase 6 used. If unclear, use `'Whole-Tel <noreply@whole-tel.com>'`
  - `to`: `'adam@whole-tel.com'`
  - `replyTo`: the submitter's email (so Adam can reply directly)
  - `subject`: `Contact form: {name}`
  - `text`: `From: {name} ({email})\n\n{message}`
- Wrap send in try/catch: return `{ success: 'Message sent! We will get back to you soon.' }` on success, `{ error: 'Failed to send message. Please try again.' }` on failure
- Return type: `{ error?: string; success?: string } | null`

**src/components/contact/ContactForm.tsx** — Client Component:
- `'use client'` directive
- Import `useActionState` from `react`, `sendContactEmail` from `@/lib/actions/contact`
- Import shadcn/ui: `Button`, `Input`, `Textarea`, `Label` — all already installed
- `useActionState(sendContactEmail, null)` for form state
- Form with `action={formAction}`:
  - Name field: Label + Input (name="name", required)
  - Email field: Label + Input (name="email", type="email", required)
  - Message field: Label + Textarea (name="message", rows={5}, required)
  - Error display: `state?.error` in red text
  - Success display: `state?.success` in brand-teal text. When success is shown, optionally clear the form (reset via key prop or form.reset)
  - Submit button: `bg-brand-teal text-white`, disabled={isPending}, text "Sending..." when pending, "Send Message" otherwise
- Styling: `space-y-4 max-w-lg` on the form
  </action>
  <verify>
    <automated>cd /Users/adamblackman/Code/whole-tel && npx next build 2>&1 | tail -20</automated>
  </verify>
  <done>Server Action validates input and sends email via Resend. ContactForm renders with proper fields and pending/error/success states. Build succeeds.</done>
</task>

<task type="auto">
  <name>Task 2: Create About Us and Contact pages in (guest) route group</name>
  <files>
    src/app/(guest)/about/page.tsx
    src/app/(guest)/contact/page.tsx
  </files>
  <action>
**src/app/(guest)/about/page.tsx** — Server Component:
- Export metadata: `{ title: 'About Us | Whole-Tel' }`
- Content sections (all static, no data fetching):
  1. **Hero banner**: Smaller than homepage hero. `bg-gradient-to-r from-brand-teal to-cyan-500 text-white py-16 px-6 text-center`. Heading: "About Whole-Tel", subheading: "The whole trip, not just a stay."
  2. **Brand story section**: Max-w-3xl centered prose. Explain the concept:
     - Whole-Tel was born from a simple idea: why book a villa and then spend hours coordinating everything else? Private chefs, boat excursions, nightlife packages, local guides — we curate it all so you can focus on what matters: the experience.
     - Our villas are handpicked for groups who want more than a vacation — they want a trip worth talking about. Every property comes with a menu of curated local add-ons you won't find on Airbnb.
  3. **Destinations section**: Brief section mentioning the three destinations (Cabo San Lucas, Puerto Vallarta, Miami) with a sentence about each.
  4. **CTA**: "Ready to plan your trip?" with Button linking to `/properties`
- Styling: Clean typography, generous spacing, brand colors. Use `text-brand-palm` for headings, `text-zinc-600` for body copy.

**src/app/(guest)/contact/page.tsx** — Server Component shell:
- Export metadata: `{ title: 'Contact Us | Whole-Tel' }`
- Layout: Two-column on desktop, stacked on mobile
  - Left column: Heading "Get in Touch", description text ("Have a question about a property, want to plan a group trip, or just want to say hello? We'd love to hear from you."), and contact info: email adam@whole-tel.com with a Mail icon from lucide-react
  - Right column: `<ContactForm />` component
- Both pages are inside `(guest)` route group so they automatically get GuestNav from the layout.
  </action>
  <verify>
    <automated>cd /Users/adamblackman/Code/whole-tel && npx next build 2>&1 | tail -20</automated>
  </verify>
  <done>About page at /about shows brand story with GuestNav. Contact page at /contact shows contact form with GuestNav. Both build successfully.</done>
</task>

</tasks>

<verification>
- `npx next build` succeeds without errors
- `/about` page renders brand story content with GuestNav
- `/contact` page renders contact form with GuestNav
- Contact form submits via Server Action and shows success/error states
- Server Action sends email to adam@whole-tel.com with replyTo set to submitter email
- Both pages are inside (guest) route group (not at app root)
</verification>

<success_criteria>
About Us page at `/about` tells the Whole-Tel brand story. Contact page at `/contact` has a working form that validates input and sends email to adam@whole-tel.com via Resend. Both pages inherit GuestNav navigation.
</success_criteria>

<output>
After completion, create `.planning/phases/07-landing-page-and-polish/07-02-SUMMARY.md`
</output>
