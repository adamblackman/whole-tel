---
phase: 08-fixes-and-rebrand
plan: 02
type: execute
wave: 1
depends_on: []
files_modified:
  - src/app/layout.tsx
  - src/app/page.tsx
  - src/components/landing/Hero.tsx
  - src/components/landing/BrandStory.tsx
  - src/components/landing/FeaturedProperties.tsx
  - src/components/landing/Testimonials.tsx
  - src/components/landing/DestinationCards.tsx
  - src/components/GuestNav.tsx
  - src/app/(guest)/about/page.tsx
  - src/app/(guest)/properties/page.tsx
  - src/app/(guest)/properties/[propertyId]/page.tsx
  - src/app/(guest)/bookings/page.tsx
  - src/app/(owner)/dashboard/page.tsx
  - src/components/dashboard/PropertyForm.tsx
  - supabase/migrations/20260302000002_seed_data.sql
  - supabase/migrations/20260308000001_rebrand_seed_data.sql
autonomous: true
requirements: [BRAND-01, BRAND-02, BRAND-03]

must_haves:
  truths:
    - "Every user-facing page says 'Whole-Tel' and 'all-inclusive hotels' -- no remaining references to 'party villas'"
    - "The hero section displays the new tagline: 'Your next unforgettable group trip starts with a Whole-Tel!'"
    - "Browser tab titles, meta descriptions, and Open Graph tags all reflect the Whole-Tel brand"
  artifacts:
    - path: "src/components/landing/Hero.tsx"
      provides: "New hero tagline and subtitle"
      contains: "Your next unforgettable group trip starts with a Whole-Tel"
    - path: "src/app/layout.tsx"
      provides: "Root metadata with Whole-Tel branding"
      contains: "Whole-Tel"
    - path: "src/app/(guest)/about/page.tsx"
      provides: "Full about page rewrite with all-inclusive hotel brand story"
      contains: "all-inclusive"
    - path: "supabase/migrations/20260308000001_rebrand_seed_data.sql"
      provides: "UPDATE migration for existing DB seed data"
      contains: "UPDATE"
  key_links:
    - from: "src/app/layout.tsx"
      to: "all pages"
      via: "metadata template title"
      pattern: "Whole-Tel"
    - from: "src/components/GuestNav.tsx"
      to: "navigation"
      via: "nav link text"
      pattern: "Browse Hotels"
---

<objective>
Rebrand the entire site from "party villas" to "Whole-Tel all-inclusive hotels." Update all user-facing copy across 15+ files, rewrite the about page and testimonials, update hero with the locked tagline, update all metadata/SEO, and update seed data.

Purpose: Establish the Whole-Tel brand identity before building new features in phases 9-11.
Output: Every page reflects the Whole-Tel all-inclusive hotel brand with no stray "villa" or "party" references.
</objective>

<execution_context>
@/Users/adamblackman/.claude/get-shit-done/workflows/execute-plan.md
@/Users/adamblackman/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/08-fixes-and-rebrand/08-CONTEXT.md
@.planning/phases/08-fixes-and-rebrand/08-RESEARCH.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Update hero, landing components, navigation, and root metadata</name>
  <files>src/app/layout.tsx, src/app/page.tsx, src/components/landing/Hero.tsx, src/components/landing/BrandStory.tsx, src/components/landing/FeaturedProperties.tsx, src/components/landing/Testimonials.tsx, src/components/landing/DestinationCards.tsx, src/components/GuestNav.tsx</files>
  <action>
**Root metadata (src/app/layout.tsx):**
- Update `title` to use "Whole-Tel" (e.g., `{ default: "Whole-Tel | All-Inclusive Group Hotels", template: "%s | Whole-Tel" }`)
- Update `description` to reference all-inclusive hotels for group travel
- Update `openGraph` title and description to match
- Remove any "party" or "villa" references

**Hero (src/components/landing/Hero.tsx):**
- Main tagline (LOCKED): "Your next unforgettable group trip starts with a Whole-Tel!"
- Subtitle: Claude's discretion -- all-inclusive focus, don't redundantly mention experiences (they're implied by "all-inclusive"). Keep it warm and exciting.
- Update any "Browse Villas" CTA buttons to "Browse Hotels"
- Update any other villa/party language

**BrandStory (src/components/landing/BrandStory.tsx):**
- Replace all "party villas" language with all-inclusive hotel group travel language
- Brand voice: warm, exciting, group-focused -- not corporate or stuffy. Think bachelor/bachelorette weekends, corporate retreats, family reunions, friend getaways.

**FeaturedProperties (src/components/landing/FeaturedProperties.tsx):**
- "Featured Villas" heading -> "Featured Hotels" or "Featured Properties"
- "party-ready" -> "all-inclusive" or "hand-picked"
- Update any other villa language

**Testimonials (src/components/landing/Testimonials.tsx):**
- Full rewrite of all testimonials (per user decision -- not keyword swap)
- Reference all-inclusive hotel group experiences
- Keep realistic, brand-aligned content
- Keep the testimonials section structure

**DestinationCards (src/components/landing/DestinationCards.tsx):**
- Keep destinations: Cabo, PV, Miami
- Update taglines/descriptions from villa language to hotel language (e.g., "Jungle villas & golden sunsets" -> something hotel-appropriate)

**GuestNav (src/components/GuestNav.tsx):**
- "Browse Villas" -> "Browse Hotels"

**Landing page (src/app/page.tsx):**
- Line 36: "Browse Villas" -> "Browse Hotels"
  </action>
  <verify>
    <automated>pnpm build && grep -ri "villa\|party" src/components/landing/ src/components/GuestNav.tsx src/app/layout.tsx src/app/page.tsx --include="*.tsx" --include="*.ts" | grep -v "PartyPopper\|node_modules" | head -20</automated>
  </verify>
  <done>Hero displays locked tagline. All landing components, nav, and root metadata use Whole-Tel all-inclusive hotel branding. No "villa" or "party" text in user-facing copy across these files (except PartyPopper icon name which is non-user-facing).</done>
</task>

<task type="auto">
  <name>Task 2: Update guest pages, owner dashboard, property form, and seed data</name>
  <files>src/app/(guest)/about/page.tsx, src/app/(guest)/properties/page.tsx, src/app/(guest)/properties/[propertyId]/page.tsx, src/app/(guest)/bookings/page.tsx, src/app/(owner)/dashboard/page.tsx, src/components/dashboard/PropertyForm.tsx, supabase/migrations/20260302000002_seed_data.sql, supabase/migrations/20260308000001_rebrand_seed_data.sql</files>
  <action>
**About page (src/app/(guest)/about/page.tsx):**
- FULL CONTENT REWRITE (per user decision -- not keyword swaps)
- New brand story centered on all-inclusive group travel with curated experiences
- Voice: warm, exciting, group-focused. Think "hand-picked all-inclusive properties for unforgettable group experiences"
- Update page metadata to match

**Properties listing (src/app/(guest)/properties/page.tsx):**
- Update h1 heading from any villa language to hotel language
- Update empty-state text
- Update metadata title/description

**Property detail (src/app/(guest)/properties/[propertyId]/page.tsx):**
- "About this villa" heading -> "About this property" or "About this hotel"

**Bookings page (src/app/(guest)/bookings/page.tsx):**
- Line 169: "Browse villas" link text -> "Browse Hotels"
- NOTE: The formatCurrency fix on this file is handled in Plan 01. Only change the copy here.

**Owner dashboard (src/app/(owner)/dashboard/page.tsx):**
- "Add your first villa" empty state text -> "Add your first property" or similar

**PropertyForm (src/components/dashboard/PropertyForm.tsx):**
- "Villa Paraiso" placeholder -> "Puerto Vallarta Casa del Mar" (location-first naming style matching Adam's pattern)
- "Describe your villa" -> "Describe your property"
- Any other villa references

**Seed data -- original file (supabase/migrations/20260302000002_seed_data.sql):**
- Rename seed properties using Adam's naming pattern (location-first):
  - PV seed property: e.g., "Puerto Vallarta Casa del Sol"
  - Miami seed property: e.g., "Miami South Beach Azure"
- DO NOT touch the real owner data (user `9d2398ac`, property "Cabo San Lucas Puesta Del Sol") -- it's already correct
- Update seed property descriptions from party villa voice to all-inclusive hotel voice
- Update add-on descriptions that reference old property names (e.g., "Villa Paraiso" in Airport Transfer description)

**New UPDATE migration (supabase/migrations/20260308000001_rebrand_seed_data.sql):**
- Create UPDATE statements for existing deployed DB rows
- Use property `id` (UUID) as the stable identifier for WHERE clauses -- read the seed SQL to get the UUIDs
- Update property names, descriptions, and add-on descriptions to match the changes made in the original seed file
- Add a comment header explaining this is a rebrand migration for existing databases
  </action>
  <verify>
    <automated>pnpm build && grep -ri "villa\|party" src/app/\(guest\)/ src/app/\(owner\)/ src/components/dashboard/ --include="*.tsx" --include="*.ts" | grep -v "PartyPopper\|node_modules" | head -20</automated>
  </verify>
  <done>About page fully rewritten with new brand story. All guest and owner pages use Whole-Tel branding. PropertyForm uses location-first placeholder. Seed SQL updated for fresh installs. New UPDATE migration created for existing databases. No "villa" or "party" text in user-facing copy across these files.</done>
</task>

<task type="auto">
  <name>Task 3: Final grep audit for stray references</name>
  <files></files>
  <action>
Run a comprehensive grep audit across the entire `src/` directory and `supabase/` directory for any remaining "villa" or "party" references:

```bash
grep -ri "villa\|party" src/ supabase/ --include="*.tsx" --include="*.ts" --include="*.sql"
```

**Expected allowable hits:**
- `PartyPopper` (Lucide icon name in AddOnCard.tsx) -- leave alone
- Any non-user-facing code comments that reference the migration itself

**Any other hits:** Fix them immediately.

Also verify:
- `grep -ri "Whole-Tel" src/app/layout.tsx` confirms root metadata has brand name
- `grep -ri "unforgettable group trip" src/components/landing/Hero.tsx` confirms locked tagline
- All page metadata exports contain "Whole-Tel" where appropriate
  </action>
  <verify>
    <automated>grep -ri "villa\|party" src/ --include="*.tsx" --include="*.ts" | grep -v "PartyPopper" | wc -l | xargs test 0 -eq && echo "PASS: No stray villa/party references" || echo "FAIL: Stray references found"</automated>
  </verify>
  <done>Zero stray "villa" or "party" references in user-facing code. Only allowable hit is the PartyPopper icon import. The Whole-Tel brand is consistent across every page.</done>
</task>

</tasks>

<verification>
- `pnpm build` succeeds
- `grep -ri "villa\|party" src/ --include="*.tsx" --include="*.ts" | grep -v "PartyPopper"` returns zero results
- Hero contains the locked tagline
- Root metadata contains "Whole-Tel"
- About page is a full rewrite (not keyword swap)
- Seed SQL has location-first property names
- New UPDATE migration exists at `supabase/migrations/20260308000001_rebrand_seed_data.sql`
</verification>

<success_criteria>
- Every user-facing page says "Whole-Tel" and "all-inclusive hotels" -- zero "party villas" references
- Hero displays: "Your next unforgettable group trip starts with a Whole-Tel!"
- Browser tabs, meta descriptions, and OG tags reflect Whole-Tel branding
- Seed data uses location-first naming pattern
- `pnpm build` passes
</success_criteria>

<output>
After completion, create `.planning/phases/08-fixes-and-rebrand/08-02-SUMMARY.md`
</output>
