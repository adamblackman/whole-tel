---
phase: 08-fixes-and-rebrand
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - src/app/(guest)/bookings/page.tsx
  - src/lib/actions/auth.ts
  - src/app/(auth)/login/page.tsx
  - src/app/(auth)/login/LoginForm.tsx
  - src/app/(auth)/signup/page.tsx
  - src/app/(auth)/owner/login/page.tsx
  - src/app/(auth)/owner/signup/page.tsx
autonomous: true
requirements: [FIX-01, FIX-02]

must_haves:
  truths:
    - "A user can sign up, log in, log out, and re-log-in without auth errors or broken redirects"
    - "Booking prices display correctly -- a $5,000 booking shows as $5,000, not $50"
  artifacts:
    - path: "src/app/(guest)/bookings/page.tsx"
      provides: "Correct currency formatting without /100 division"
      contains: "formatCurrency"
    - path: "src/lib/actions/auth.ts"
      provides: "Working auth actions for all flows"
  key_links:
    - from: "src/lib/actions/auth.ts"
      to: "src/app/(auth)/*"
      via: "server action imports"
      pattern: "signIn|signUp|signOut"
---

<objective>
Fix the two known bugs: the formatCurrency /100 error on the bookings page, and audit+fix all auth flows (guest and owner signup/login/logout).

Purpose: Clear blocking bugs before rebrand work begins. Users currently see incorrect prices and may hit auth errors.
Output: Working auth lifecycle and correct currency display.
</objective>

<execution_context>
@/Users/adamblackman/.claude/get-shit-done/workflows/execute-plan.md
@/Users/adamblackman/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/08-fixes-and-rebrand/08-CONTEXT.md
@.planning/phases/08-fixes-and-rebrand/08-RESEARCH.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Fix formatCurrency bug and audit auth flows</name>
  <files>src/app/(guest)/bookings/page.tsx, src/lib/actions/auth.ts, src/app/(auth)/login/page.tsx, src/app/(auth)/login/LoginForm.tsx, src/app/(auth)/signup/page.tsx, src/app/(auth)/owner/login/page.tsx, src/app/(auth)/owner/signup/page.tsx</files>
  <action>
**formatCurrency fix (FIX-02):**
- In `src/app/(guest)/bookings/page.tsx` around line 63, the inline `formatCurrency` function divides `cents / 100` but values are stored in dollars, not cents. Remove the `/ 100` division so `$5000` displays as `$5,000` not `$50.00`.
- Search the entire codebase for any other `/100` or `cents` patterns that might have the same bug: `grep -rn "/ 100\|/100\|cents" src/ --include="*.ts" --include="*.tsx"`. Fix any additional occurrences.

**Auth flow audit and fixes (FIX-01):**
- Read all 6 auth page files + `src/lib/actions/auth.ts` + `src/middleware.ts`
- Trace the full flow for each path:
  1. Guest signup -> should auto-login -> redirect to /properties
  2. Guest login -> redirect to /properties (or returnTo URL)
  3. Guest logout -> redirect to /
  4. Owner signup -> should auto-login -> redirect to /dashboard
  5. Owner login -> redirect to /dashboard
  6. Owner logout -> redirect to /
- For each flow, verify: correct Supabase auth method called, redirect URL is correct, error states handled and displayed, no redirect loops between middleware and page
- Fix any issues found. Common bugs to check:
  - `getSession()` used in Server Components (should use `getClaims()` per CLAUDE.md)
  - Middleware redirect logic conflicting with page-level redirects
  - Missing error handling on auth actions
  - Stale session after logout (cookie not cleared)
- Supabase email confirmation stays OFF (dev mode) -- do not change this
  </action>
  <verify>
    <automated>pnpm build</automated>
  </verify>
  <done>formatCurrency displays dollar values correctly without /100 division. All 6 auth flows (guest signup/login/logout, owner signup/login/logout) have been audited, issues identified and fixed, and the code builds successfully.</done>
</task>

</tasks>

<verification>
- `pnpm build` succeeds with no errors
- `grep -rn "/ 100\|/100" src/app/(guest)/bookings/page.tsx` returns no hits on the formatCurrency line
- Auth action files have proper error handling and correct redirect URLs
</verification>

<success_criteria>
- formatCurrency shows $5,000 for a 5000-dollar booking (no /100 division)
- All auth flows have been audited and fixed -- no broken redirects, no error state issues
- `pnpm build` passes
</success_criteria>

<output>
After completion, create `.planning/phases/08-fixes-and-rebrand/08-01-SUMMARY.md`
</output>
