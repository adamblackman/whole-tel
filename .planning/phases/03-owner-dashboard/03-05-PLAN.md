---
phase: 03-owner-dashboard
plan: 05
type: verify
wave: 4
depends_on:
  - 03-04
files_modified: []
autonomous: false
requirements:
  - OWNER-01
  - OWNER-02
  - OWNER-03
  - OWNER-04
  - OWNER-05
  - OWNER-06
  - OWNER-07
  - OWNER-08
must_haves:
  truths:
    - "An owner can create a new property listing with all required fields and it appears in the dashboard list"
    - "An owner can edit an existing property listing and changes persist after page reload"
    - "An owner can delete a property listing with confirmation and it disappears from the dashboard"
    - "An owner can upload photos to a property and they display correctly in the photo grid"
    - "An owner can create, edit, and delete add-on experiences with per-person or per-booking pricing"
    - "An owner can view bookings for their properties (or see an empty state if no bookings exist)"
    - "An owner cannot see or modify another owner's properties — data isolation works at both UI and database layers"
    - "All forms show field-level validation errors for invalid input"
    - "All forms show pending/loading states during Server Action execution"
  artifacts: []
  key_links: []
---

<objective>
Human verification checkpoint: test all OWNER requirements end-to-end in the browser.

Purpose: Before marking Phase 3 complete, manually verify that every OWNER requirement (01-08) works correctly in a running application. This catches issues that automated TypeScript checks cannot — visual rendering, form validation UX, photo upload flow, and data isolation between owners.
Output: Verified confirmation that all 8 OWNER requirements are satisfied, or a list of issues to fix.
</objective>

<execution_context>
@/Users/adamblackman/.claude/get-shit-done/workflows/execute-plan.md
@/Users/adamblackman/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/phases/03-owner-dashboard/03-RESEARCH.md
@.planning/phases/03-owner-dashboard/03-04-SUMMARY.md
</context>

<tasks>

<task type="human">
  <name>Task 1: Verify property CRUD (OWNER-01, OWNER-02, OWNER-03)</name>
  <action>
Start the dev server (`npm run dev`) and log in as an owner at /owner/login.

**OWNER-01 — Create property:**
1. Navigate to /dashboard
2. Click "Add Property"
3. Fill in all required fields: name, location (select one of the three destinations), bedrooms, bathrooms, max_guests, nightly_rate
4. Select some amenities from the checkbox group
5. Submit the form
6. Verify: redirected to /dashboard, new property card appears in the list

**OWNER-02 — Edit property:**
1. Click on the property card to view detail
2. Click the "Edit" button/link
3. Verify: form is pre-filled with all existing data (including amenities checkboxes)
4. Change the name and nightly_rate
5. Submit the form
6. Verify: redirected to property detail page, changes are reflected

**OWNER-03 — Delete property:**
1. On the property detail page, scroll to the "Danger Zone" section
2. Click the delete button
3. Verify: AlertDialog opens with property name and cascade warning
4. Click "Delete" to confirm
5. Verify: redirected to /dashboard, property no longer appears

Also test validation:
- Try to create a property with empty name — field-level error should appear
- Try to set bedrooms to 0 — validation error should appear
  </action>
  <verify>
    <human>Owner can create, edit, and delete a property. Form validation prevents invalid submissions.</human>
  </verify>
  <done>OWNER-01, OWNER-02, OWNER-03 verified: property CRUD works end-to-end with validation.</done>
</task>

<task type="human">
  <name>Task 2: Verify photo upload and add-on management (OWNER-04, OWNER-05, OWNER-06)</name>
  <action>
Create a new property (or use an existing one).

**OWNER-04 — Photo upload:**
1. Navigate to the property detail page
2. In the Photos section, click the upload button
3. Select an image file (try both a small and a large image > 1 MB)
4. Verify: upload completes, photo appears in the grid
5. Upload a second photo — verify both display
6. Click the delete button on a photo — verify it disappears from the grid
7. Refresh the page — verify photos persist

**OWNER-05 — Add-on CRUD:**
1. In the Add-ons section, click "Add Experience"
2. Fill in: name (e.g., "Private Chef"), description, price (e.g., 150)
3. Select pricing_unit: "Per Person"
4. Submit — verify add-on appears in the list with correct pricing badge
5. Click Edit on the add-on — verify form pre-fills
6. Change the price and pricing_unit to "Per Booking" — submit
7. Verify changes persist
8. Click Delete on the add-on — confirm — verify it disappears

**OWNER-06 — Pricing unit:**
1. Create two add-ons: one "Per Person" and one "Per Booking"
2. Verify both display the correct pricing unit label/badge
3. Verify the Select dropdown shows both options correctly
  </action>
  <verify>
    <human>Photos upload directly to Supabase Storage (not through server). Add-ons support create, edit, delete with per-person and per-booking pricing.</human>
  </verify>
  <done>OWNER-04, OWNER-05, OWNER-06 verified: photo upload flow works, add-on CRUD works with both pricing units.</done>
</task>

<task type="human">
  <name>Task 3: Verify bookings view and data isolation (OWNER-07, OWNER-08)</name>
  <action>
**OWNER-07 — View bookings:**
1. Navigate to /dashboard/bookings
2. If no bookings exist yet (expected — no guests have booked in this phase): verify the empty state message appears
3. If seed data includes bookings: verify bookings display in the table with correct columns (property name, dates, guests, total, status)
4. Verify the dashboard header has a "Bookings" nav link that navigates to this page

**OWNER-08 — Data isolation (most critical):**
1. Log out of the current owner account
2. Sign up a second owner account at /owner/signup (use a different email)
3. Log in as the second owner at /owner/login
4. Navigate to /dashboard
5. Verify: the dashboard is empty — no properties from the first owner appear
6. Create a property as the second owner
7. Navigate to /dashboard/bookings — verify no bookings appear (the first owner's bookings are not visible)
8. Log out and log back in as the first owner
9. Verify: only the first owner's properties appear — the second owner's property is not visible
10. Try to directly navigate to the second owner's property detail page (if you know the ID) — verify a 404 or empty result

Also verify guest access restriction:
- Log in as a guest account
- Try to navigate to /dashboard — verify redirect to / (not /login)
  </action>
  <verify>
    <human>Bookings page shows owner's bookings (or empty state). Different owner accounts see only their own data. Guest accounts cannot access /dashboard.</human>
  </verify>
  <done>OWNER-07, OWNER-08 verified: bookings view works, data isolation enforced at both UI and database layers, guest accounts blocked from dashboard.</done>
</task>

</tasks>

<verification>
All 8 OWNER requirements manually verified:
- OWNER-01: Create property with all fields works
- OWNER-02: Edit property with pre-filled form works
- OWNER-03: Delete property with confirmation dialog works
- OWNER-04: Photo upload via signed URL works (including > 1 MB files)
- OWNER-05: Add-on create, edit, delete works
- OWNER-06: Per-person and per-booking pricing both work
- OWNER-07: Bookings view shows owner's bookings
- OWNER-08: Data isolation verified with two owner accounts + guest blocked
</verification>

<success_criteria>
- All 8 OWNER requirements pass manual testing
- No JavaScript console errors during testing
- Forms show validation errors and pending states correctly
- Photo upload works for files > 1 MB (signed URL pattern verified)
- Two separate owner accounts see only their own data
</success_criteria>

<output>
After completion, create `.planning/phases/03-owner-dashboard/03-05-SUMMARY.md` using the summary template. Mark Phase 3 as complete in STATE.md and ROADMAP.md.
</output>
