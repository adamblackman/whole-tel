---
phase: 02-auth
plan: 04
type: execute
wave: 3
depends_on:
  - 02-02
  - 02-03
files_modified: []
autonomous: false
requirements:
  - AUTH-01
  - AUTH-02
  - AUTH-03
  - AUTH-04
  - AUTH-05
  - AUTH-06
user_setup:
  - service: supabase-auth
    why: "Email confirmation must be disabled during development so signup-then-login works without a real SMTP server"
    dashboard_config:
      - task: "Disable email confirmations for dev testing"
        location: "Supabase Dashboard → Authentication → Providers → Email → toggle 'Confirm email' OFF"
      - task: "Verify handle_new_user trigger exists (from Phase 1 migration)"
        location: "Supabase Dashboard → SQL Editor → run: SELECT routine_name FROM information_schema.routines WHERE routine_name = 'handle_new_user';"
must_haves:
  truths:
    - "A new guest can sign up at /signup and immediately log in without email confirmation"
    - "A logged-in guest can log out from the dashboard using LogoutButton"
    - "A new owner can sign up at /owner/signup and log in at /owner/login landing on /dashboard"
    - "A guest who navigates directly to /dashboard is redirected away"
    - "Guest and owner auth pages are visually distinct (separate URLs, different card copy)"
  artifacts:
    - path: "src/proxy.ts"
      provides: "Session refresh active on all routes"
    - path: "src/lib/actions/auth.ts"
      provides: "All 5 auth Server Actions"
    - path: "src/app/(auth)/login/page.tsx"
      provides: "Guest login"
    - path: "src/app/(auth)/signup/page.tsx"
      provides: "Guest signup"
    - path: "src/app/(auth)/owner/login/page.tsx"
      provides: "Owner login"
    - path: "src/app/(auth)/owner/signup/page.tsx"
      provides: "Owner signup"
    - path: "src/app/(owner)/dashboard/layout.tsx"
      provides: "Protected owner dashboard"
  key_links:
    - from: "/signup"
      to: "auth.users + profiles table"
      via: "signUpGuest Server Action → handle_new_user trigger"
      pattern: "profiles row with role='guest' exists after signup"
    - from: "/owner/login"
      to: "/dashboard"
      via: "signInAsOwner → profiles.role check → redirect"
      pattern: "dashboard only reachable after owner login"
---

<objective>
Human verification of all Phase 2 auth flows end-to-end — confirming that guest signup/login/logout, owner signup/login/dashboard access, and route protection all work correctly.

Purpose: This checkpoint runs AFTER all automation is complete and validates the full auth system works in a real browser against the Supabase backend. Some steps require user action (Supabase Dashboard toggles) that cannot be automated.
Output: Confirmed working auth system; any bugs logged for gap closure.
</objective>

<execution_context>
@/Users/adamblackman/.claude/get-shit-done/workflows/execute-plan.md
@/Users/adamblackman/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/02-auth/02-01-SUMMARY.md
@.planning/phases/02-auth/02-02-SUMMARY.md
@.planning/phases/02-auth/02-03-SUMMARY.md
</context>

<tasks>

<task type="checkpoint:human-action" gate="blocking">
  <name>Task 1: Supabase setup — disable email confirmation + verify trigger</name>
  <files>none</files>
  <action>
Before testing auth flows, two Supabase Dashboard actions are required that Claude cannot automate (no management API access for this project):

**Step 1: Disable email confirmation (required for dev testing)**
1. Open Supabase Dashboard → Authentication → Providers → Email
2. Toggle "Confirm email" OFF
3. Save

Without this, signUp succeeds but signInWithPassword fails with "Email not confirmed" — the test flows won't work.

**Step 2: Verify the Phase 1 handle_new_user trigger exists**
1. Open Supabase Dashboard → SQL Editor
2. Run:
   ```sql
   SELECT routine_name FROM information_schema.routines
   WHERE routine_name = 'handle_new_user';
   ```
3. Should return 1 row. If 0 rows → the Phase 1 migration hasn't been applied yet (see 01-01-SUMMARY.md User Setup section) — apply it before proceeding.

**Step 3: Start the dev server**
```bash
npm run dev
```
The app should start at http://localhost:3000.
  </action>
  <verify>User confirms dev server is running and email confirmation is disabled</verify>
  <done>Dev server running at localhost:3000; Supabase email confirmation disabled; handle_new_user trigger confirmed present</done>
  <resume-signal>Type "ready" when dev server is running and Supabase email confirmation is disabled</resume-signal>
</task>

<task type="checkpoint:human-verify" gate="blocking">
  <name>Task 2: Verify all auth flows end-to-end</name>
  <files>none</files>
  <action>
Human verifies all 6 auth flows below in a browser against the live dev server.
  </action>
  <what-built>
Complete auth system: guest signup/login/logout, owner signup/login, dashboard route protection, and visually distinct auth pages.
  </what-built>
  <how-to-verify>
Run through these flows in order. Each should behave exactly as described:

**Flow 1 — Guest Signup**
1. Navigate to http://localhost:3000/signup
2. Fill in: Name "Test Guest", Email "guest@test.com", Password "testpass123"
3. Click "Create account"
4. Expected: Redirected to / (homepage)
5. Verify in Supabase Dashboard → Authentication → Users: guest@test.com appears

**Flow 2 — Guest Login**
1. Navigate to http://localhost:3000/login
2. Sign in with guest@test.com / testpass123
3. Expected: Redirected to /

**Flow 3 — Guest Cannot Access Dashboard**
1. While logged in as guest@test.com, navigate to http://localhost:3000/dashboard
2. Expected: Redirected away (to / or /login, NOT shown the dashboard content)

**Flow 4 — Guest Logout**
1. Navigate to /dashboard (will redirect), or find a LogoutButton on any page
2. Click "Sign out"
3. Expected: Redirected to /, logged-out state

**Flow 5 — Owner Signup**
1. Navigate to http://localhost:3000/owner/signup
2. Fill in: Email "owner@test.com", Password "ownerpass123"
3. Click "Create owner account"
4. Expected: Redirected to /owner/login with "Account created" message visible
5. Verify in Supabase Dashboard → Table Editor → profiles: owner@test.com row has role = 'owner'

**Flow 6 — Owner Login and Dashboard Access**
1. Navigate to http://localhost:3000/owner/login
2. Sign in with owner@test.com / ownerpass123
3. Expected: Redirected to /dashboard with placeholder "Your Properties" page and sign-out button in header

**Visual Check**
- /login: teal gradient background, "Welcome back" title, links to /signup and /owner/login
- /owner/login: same centered layout, amber "HOST" badge, "Owner Portal" title, link back to guest login
- The two login pages are clearly visually distinct
  </how-to-verify>
  <verify>All 6 flows pass as described above</verify>
  <done>All auth flows verified working; auth pages visually distinct; no console errors</done>
  <resume-signal>Type "approved" if all flows work. Describe any failures (e.g., "Flow 3 failed — guest can access /dashboard") for gap closure.</resume-signal>
</task>

</tasks>

<verification>
All 6 flows above pass without errors.
</verification>

<success_criteria>
- Guest can sign up, log in, and log out
- Owner can sign up with role 'owner', log in, and reach /dashboard
- Guests cannot access /dashboard (route protection enforced)
- Auth pages are visually distinct with clear cross-links between guest and owner paths
- No JavaScript console errors during any flow
</success_criteria>

<output>
After completion, create `.planning/phases/02-auth/02-04-SUMMARY.md` using the summary template.
</output>
