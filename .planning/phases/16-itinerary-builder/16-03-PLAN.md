---
phase: 16-itinerary-builder
plan: 03
type: execute
wave: 3
depends_on: ["16-02"]
files_modified: []
autonomous: false
requirements:
  - ITIN-01
  - ITIN-02
  - ITIN-03
  - ITIN-04
  - ITIN-05
  - ITIN-06
  - ITIN-07

must_haves:
  truths:
    - "Owner activity management works end-to-end"
    - "Guest itinerary builder works end-to-end"
    - "Auto-save persists across navigation"
  artifacts: []
  key_links: []
---

<objective>
Human verification of the complete itinerary builder feature: owner activity configuration and guest calendar scheduling.

Purpose: Visual and functional verification that cannot be automated — calendar rendering, interaction flows, time slot enforcement UX, and auto-save behavior.

Output: Verified feature or issues to fix.
</objective>

<execution_context>
@/Users/adamblackman/.claude/get-shit-done/workflows/execute-plan.md
@/Users/adamblackman/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/16-itinerary-builder/16-01-SUMMARY.md
@.planning/phases/16-itinerary-builder/16-02-SUMMARY.md
</context>

<tasks>

<task type="checkpoint:human-verify" gate="blocking">
  <name>Task 1: Verify complete itinerary builder feature</name>
  <files></files>
  <action>
Present the verification steps below to the user and wait for approval.
  </action>
  <what-built>
Complete itinerary builder feature:
- Owner activity management at /dashboard/properties/[propertyId]/activities (create, edit, delete activities with time slots)
- Guest itinerary calendar at /bookings/[bookingId]/itinerary (FullCalendar with date scoping, custom events, property activity scheduling, auto-save)
- "Plan Itinerary" link on booking cards for confirmed bookings
  </what-built>
  <how-to-verify>
**Owner flow (ITIN-01):**
1. Log in as owner, navigate to a property edit page
2. Click "Manage Activities" link
3. Add a new activity (e.g., "Boat Ride", 150 min, slots: 9:30-12:00 and 13:00-16:00)
4. Add another activity (e.g., "Sunset Dinner", 120 min, slot: 17:00-21:00)
5. Verify both activities appear in the list
6. Edit one activity (change name or add a slot), verify it saves
7. Delete one activity, verify it disappears

**Guest flow (ITIN-02 through ITIN-07):**
1. Log in as guest with a confirmed booking
2. On the bookings page, verify "Plan Itinerary" link appears on the confirmed booking card
3. Click "Plan Itinerary" — calendar should load showing the booking date range
4. Verify calendar is scoped to booking dates (cannot navigate before check-in or after check-out)
5. Click a time slot on the calendar — verify choice between "Custom Event" and "Property Activity"
6. Add a custom event (e.g., "Pool Day" 11:00-17:00) — verify it appears on the calendar
7. Add a property activity — verify only the owner-configured time slots are available to select
8. Navigate away from the page (go to /bookings), then return — verify all events are still there (auto-save)
9. Delete an event by clicking on it — verify it disappears from the calendar

**Expected result:** All itinerary flows work. Calendar renders correctly. Auto-save persists data.
  </how-to-verify>
  <verify>User types "approved" or describes issues</verify>
  <done>All ITIN-01 through ITIN-07 requirements verified by human testing</done>
  <resume-signal>Type "approved" or describe issues to fix</resume-signal>
</task>

</tasks>

<verification>
Human confirms all ITIN-01 through ITIN-07 requirements are met via manual testing.
</verification>

<success_criteria>
User approves the complete itinerary builder feature.
</success_criteria>

<output>
After completion, create `.planning/phases/16-itinerary-builder/16-03-SUMMARY.md`
</output>
