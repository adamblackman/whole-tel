---
phase: 12-branding-copy-amenities-schema
plan: 02
type: execute
wave: 1
depends_on: []
files_modified:
  - src/components/GuestNav.tsx
  - src/app/layout.tsx
  - src/app/(guest)/about/page.tsx
  - src/app/(guest)/bookings/page.tsx
  - src/app/(guest)/properties/page.tsx
  - src/app/(guest)/contact/page.tsx
  - src/app/(auth)/signup/SignupForm.tsx
  - src/app/(auth)/login/LoginForm.tsx
  - src/components/browse/DestinationFilter.tsx
  - src/components/browse/PropertyListingCard.tsx
autonomous: true
requirements: [BRAND-02, BRAND-07, BRAND-08]

must_haves:
  truths:
    - "All public-facing 'Whole-Tel' instances across non-landing pages carry the TM symbol"
    - "No 'Browse Hotels' or 'Featured Hotels' text appears anywhere in the app"
    - "Property cards display bed count by type (King x2, Queen x1) instead of generic bed count"
    - "Miami is removed from destination filter and valid destinations list"
    - "Contact page metadata and auth page copy include TM-branded Whole-Tel references"
  artifacts:
    - path: "src/components/browse/PropertyListingCard.tsx"
      provides: "Bed config breakdown display on property cards"
      contains: "bed_config"
    - path: "src/components/GuestNav.tsx"
      provides: "TM-branded nav links"
      contains: "Whole-Tel"
    - path: "src/app/(guest)/about/page.tsx"
      provides: "Updated about page with TM branding and no hotel language"
      contains: "Whole-Tel"
    - path: "src/app/(guest)/contact/page.tsx"
      provides: "TM-branded contact page metadata"
      contains: "Whole-Tel"
    - path: "src/app/(auth)/signup/SignupForm.tsx"
      provides: "TM-branded signup copy"
      contains: "Whole-Tel"
    - path: "src/app/(auth)/login/LoginForm.tsx"
      provides: "TM-branded login copy"
      contains: "Whole-Tel"
  key_links:
    - from: "src/app/(guest)/properties/page.tsx"
      to: "src/components/browse/PropertyListingCard.tsx"
      via: "bed_config included in select query and passed as prop"
      pattern: "bed_config"
    - from: "src/components/browse/DestinationFilter.tsx"
      to: "src/app/(guest)/properties/page.tsx"
      via: "DESTINATIONS array matches VALID_DESTINATIONS"
      pattern: "Cabo San Lucas|Puerto Vallarta"
---

<objective>
Apply TM branding and "hotel" removal across all non-landing pages, and add bed config display to property cards.

Purpose: Ensure brand consistency across the entire app (not just the homepage) and surface useful bed type information on property listing cards.
Output: All public pages show TM-branded "Whole-Tel" text, no stale "hotel" language remains, property cards show bed breakdown.
</objective>

<execution_context>
@/Users/adamblackman/.claude/get-shit-done/workflows/execute-plan.md
@/Users/adamblackman/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/12-branding-copy-amenities-schema/12-CONTEXT.md
@.planning/phases/12-branding-copy-amenities-schema/12-RESEARCH.md

@src/components/GuestNav.tsx
@src/app/layout.tsx
@src/app/(guest)/about/page.tsx
@src/app/(guest)/bookings/page.tsx
@src/app/(guest)/properties/page.tsx
@src/app/(guest)/contact/page.tsx
@src/app/(auth)/signup/SignupForm.tsx
@src/app/(auth)/login/LoginForm.tsx
@src/components/browse/DestinationFilter.tsx
@src/components/browse/PropertyListingCard.tsx

<interfaces>
<!-- Current PropertyListingCard interface that needs bed_config added -->
From src/components/browse/PropertyListingCard.tsx:
```typescript
interface PropertyListingCardProps {
  property: {
    id: string
    name: string
    location: string
    bedrooms: number
    bathrooms: number
    max_guests: number
    nightly_rate: number
    property_photos: Array<{ id: string; storage_path: string; display_order: number }>
  }
}
```

<!-- bed_config JSONB shape from migration 20260308000002 -->
bed_config default: {"king":0,"queen":0,"double":0,"twin":0,"bunk":0}
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: TM branding and hotel removal across all non-landing pages</name>
  <files>src/components/GuestNav.tsx, src/app/layout.tsx, src/app/(guest)/about/page.tsx, src/app/(guest)/bookings/page.tsx, src/app/(guest)/properties/page.tsx, src/app/(guest)/contact/page.tsx, src/app/(auth)/signup/SignupForm.tsx, src/app/(auth)/login/LoginForm.tsx, src/components/browse/DestinationFilter.tsx</files>
  <action>
**GuestNav.tsx:**
- Change nav brand text "Whole-Tel" to "Whole-Tel&trade;"
- Change "Browse Hotels" link text to "Browse Whole-Tels&trade;"

**layout.tsx:**
- Update metadata descriptions: replace "All-Inclusive Group Hotels" with appropriate Custom-Inclusive branding (e.g., "Custom-Inclusive Group Getaways")
- Update OpenGraph descriptions similarly
- Do NOT add TM to the `title.template` string `"%s | Whole-Tel"` -- this is functional metadata, not public-facing marketing copy per research anti-pattern guidance

**about/page.tsx:**
- Change heading "About Whole-Tel" to "About Whole-Tel&trade;"
- Rewrite paragraphs: replace "all-inclusive hotels" framing with Custom-Inclusive model language
- Update destinations section: remove Miami from listed destinations or mark as "Coming Soon"
- Change "Browse our hand-picked hotels" to updated copy referencing Whole-Tels
- Change "Browse Hotels" CTA to "Browse Whole-Tels&trade;"
- Update metadata description

**bookings/page.tsx:**
- Change "Browse Hotels" link/button text to "Browse Whole-Tels&trade;"
- Apply TM to any "Whole-Tel" references in empty state or success messages

**properties/page.tsx:**
- Change metadata title from "Browse Hotels" to "Browse Whole-Tels&trade;"
- Remove Miami from `VALID_DESTINATIONS` array (only Cabo San Lucas, Puerto Vallarta remain)
- Change h1 "Browse Hotels" to "Browse Whole-Tels&trade;"
- Update subtitle to remove Miami reference and "hotels" language
- Search for any remaining "hotels" text using grep and replace with "Whole-Tels" (do NOT rely on line numbers -- find text by content matching)

**contact/page.tsx:**
- Update metadata title from "Contact Us | Whole-Tel" to "Contact Us | Whole-Tel&trade;" (or keep without TM if it's in `title.template` format -- check the actual metadata pattern)
- Search the file for any "Whole-Tel" references in visible page copy and add TM symbol
- Search for any "hotel" references and replace with appropriate branding

**Auth pages -- SignupForm.tsx:**
- Change CardDescription "Join Whole-Tel to start booking" to "Join Whole-Tel&trade; to start booking"

**Auth pages -- LoginForm.tsx:**
- Change "New to Whole-Tel?" to "New to Whole-Tel&trade;?"

**DestinationFilter.tsx:**
- Remove Miami from the `DESTINATIONS` array (only Cabo San Lucas and Puerto Vallarta remain as filterable destinations)

IMPORTANT: Miami must be removed from ALL THREE places (DestinationCards in Plan 01, VALID_DESTINATIONS in properties/page.tsx, DESTINATIONS in DestinationFilter.tsx) to avoid stranded filter state. This task handles the latter two.
  </action>
  <verify>
    <automated>cd /Users/adamblackman/Code/whole-tel && npx tsc --noEmit 2>&1 | head -30 && echo "---" && grep -rn "Browse Hotels" src/ --include="*.tsx" || echo "No 'Browse Hotels' found (good)"</automated>
  </verify>
  <done>All non-landing pages show TM-branded Whole-Tel text, no "Browse Hotels" or "Featured Hotels" text remains anywhere in src/, Miami removed from destination filter and valid destinations, contact page and auth pages updated with TM branding</done>
</task>

<task type="auto">
  <name>Task 2: Add bed config display to PropertyListingCard</name>
  <files>src/components/browse/PropertyListingCard.tsx, src/app/(guest)/properties/page.tsx</files>
  <action>
**PropertyListingCard.tsx:**
- Add `bed_config: Record<string, number>` to the property interface (required field, not optional -- TypeScript will catch any query that misses it)
- Create a `formatBedConfig` helper function at the top of the file:
  ```typescript
  const BED_LABELS: Record<string, string> = { king: 'King', queen: 'Queen', double: 'Double', twin: 'Twin', bunk: 'Bunk' }
  function formatBedConfig(bedConfig: Record<string, number>): string {
    return Object.entries(bedConfig)
      .filter(([, count]) => count > 0)
      .map(([type, count]) => `${BED_LABELS[type] ?? type} x${count}`)
      .join(', ')
  }
  ```
- Replace the current `{property.bedrooms} bed` display with the formatted bed config string
- If formatBedConfig returns empty string (all zeros), fall back to `{property.bedrooms} bed`
- Keep the BedDouble icon

**properties/page.tsx (browse page):**
- Add `bed_config` to the explicit `.select()` column list in the Supabase query
- The homepage page.tsx uses `*` so bed_config is already included there
  </action>
  <verify>
    <automated>cd /Users/adamblackman/Code/whole-tel && npx tsc --noEmit 2>&1 | head -30</automated>
  </verify>
  <done>Property cards display "King x2, Queen x1" format instead of generic "N bed", bed_config is included in browse page query, TypeScript compiles clean</done>
</task>

</tasks>

<verification>
- `npx tsc --noEmit` passes with no errors
- `grep -rn "Browse Hotels" src/` returns no results
- `grep -rn "Featured Hotels" src/` returns no results
- `grep -rn "Miami" src/components/browse/DestinationFilter.tsx` returns no results
- `grep -rn "bed_config" src/components/browse/PropertyListingCard.tsx` returns matches
</verification>

<success_criteria>
- Every public-facing "Whole-Tel" in non-landing pages carries TM symbol
- Zero instances of "Browse Hotels" or "Featured Hotels" in src/
- Miami removed from DestinationFilter and VALID_DESTINATIONS
- Contact page metadata includes TM-branded Whole-Tel
- Auth pages (signup, login) include TM-branded Whole-Tel
- Property listing cards show bed type breakdown (King x2, Queen x1)
- Browse page query includes bed_config in select
- TypeScript compiles cleanly
</success_criteria>

<output>
After completion, create `.planning/phases/12-branding-copy-amenities-schema/12-02-SUMMARY.md`
</output>
