---
phase: 04-guest-browsing
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - package.json
  - package-lock.json
  - src/components/ui/calendar.tsx
  - src/app/globals.css
  - src/components/GuestNav.tsx
  - src/app/(guest)/layout.tsx
  - src/app/(guest)/properties/page.tsx
autonomous: true
requirements:
  - PAGE-05
  - PAGE-04

must_haves:
  truths:
    - "yet-another-react-lightbox is installed and importable"
    - "shadcn Calendar component exists at src/components/ui/calendar.tsx"
    - "Brand color CSS variables are defined in globals.css"
    - "A GuestNav component renders a header with logo and login link"
    - "The (guest) route group layout wraps properties pages with GuestNav"
  artifacts:
    - path: "src/components/ui/calendar.tsx"
      provides: "shadcn Calendar component wrapping react-day-picker v9"
    - path: "src/components/GuestNav.tsx"
      provides: "Guest-facing navigation header"
    - path: "src/app/(guest)/layout.tsx"
      provides: "Route group layout applying GuestNav to guest pages"
    - path: "src/app/globals.css"
      provides: "Brand color tokens for tropical chill aesthetic"
  key_links:
    - from: "src/app/(guest)/layout.tsx"
      to: "src/components/GuestNav.tsx"
      via: "import and render"
      pattern: "import.*GuestNav"
---

<objective>
Install Phase 4 dependencies (yet-another-react-lightbox, shadcn Calendar), establish the tropical chill brand color palette in globals.css, and create the guest-facing navigation layout.

Purpose: Every subsequent plan in Phase 4 depends on these dependencies, brand tokens, and the guest layout wrapper being in place. This is the foundation wave.

Output: Installed packages, Calendar UI component, brand CSS variables, GuestNav component, (guest) route group layout with a placeholder properties page to confirm the layout renders.
</objective>

<execution_context>
@/Users/adamblackman/.claude/get-shit-done/workflows/execute-plan.md
@/Users/adamblackman/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/04-guest-browsing/04-RESEARCH.md

<interfaces>
<!-- Key types and contracts the executor needs. -->

From src/app/globals.css:
- Currently all-neutral oklch palette (no brand colors)
- Uses @theme inline {} block for Tailwind v4 custom properties
- :root {} block defines shadcn CSS variables

From src/app/layout.tsx:
- Root layout renders {children} with no nav header
- Uses Geist and Geist_Mono fonts
- Metadata: "Whole-Tel — Party Villas"

From src/components/LogoutButton.tsx:
- Named export LogoutButton — ghost button calling signOut action
- Can be used in GuestNav for authenticated users (optional, keep simple for now)
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Install dependencies and add shadcn Calendar</name>
  <files>package.json, package-lock.json, src/components/ui/calendar.tsx</files>
  <action>
1. Install yet-another-react-lightbox:
   ```bash
   cd /Users/adamblackman/Code/whole-tel && npm install yet-another-react-lightbox
   ```

2. Add shadcn Calendar component (this auto-installs react-day-picker as a peer dependency):
   ```bash
   cd /Users/adamblackman/Code/whole-tel && npx shadcn@latest add calendar --yes
   ```

3. Verify both are installed by checking package.json for `yet-another-react-lightbox` and `react-day-picker`, and that `src/components/ui/calendar.tsx` exists.

Note: Do NOT install React Bits via npm — it is a copy-paste library, not an npm package. React Bits components (BlurText) will be handled in later plans if needed, or replaced with simple CSS animations to avoid install complexity.
  </action>
  <verify>
    <automated>cd /Users/adamblackman/Code/whole-tel && node -e "require('yet-another-react-lightbox'); console.log('lightbox OK')" && node -e "require('react-day-picker'); console.log('day-picker OK')" && test -f src/components/ui/calendar.tsx && echo "calendar component OK"</automated>
  </verify>
  <done>yet-another-react-lightbox and react-day-picker are in node_modules and importable. shadcn Calendar component exists at src/components/ui/calendar.tsx.</done>
</task>

<task type="auto">
  <name>Task 2: Add brand color palette and create GuestNav + (guest) layout</name>
  <files>src/app/globals.css, src/components/GuestNav.tsx, src/app/(guest)/layout.tsx, src/app/(guest)/properties/page.tsx</files>
  <action>
1. **Brand colors in globals.css**: Add custom brand CSS variables to the `:root` block in `src/app/globals.css`. Add these AFTER the existing shadcn variables (do NOT replace them):

```css
/* Whole-Tel brand palette — tropical chill */
--brand-amber: oklch(0.78 0.15 75);
--brand-teal: oklch(0.65 0.14 185);
--brand-sand: oklch(0.92 0.04 85);
--brand-palm: oklch(0.42 0.12 150);
```

Also add them to the `@theme inline {}` block so Tailwind can use them as utility classes:
```css
--color-brand-amber: var(--brand-amber);
--color-brand-teal: var(--brand-teal);
--color-brand-sand: var(--brand-sand);
--color-brand-palm: var(--brand-palm);
```

2. **GuestNav component** at `src/components/GuestNav.tsx`:
   - Server Component (no 'use client' needed — it's just links)
   - Renders a `<header>` with `max-w-7xl mx-auto px-4 py-4 flex items-center justify-between`
   - Left: Link to "/" with "Whole-Tel" as bold text in brand-teal color (`text-brand-teal font-bold text-xl`)
   - Right: Link to "/properties" labeled "Browse Villas" and Link to "/login" labeled "Log in" using Button component (variant="ghost")
   - Mobile-responsive: flex-wrap or hidden nav items behind a simple layout (keep it minimal — no hamburger menu for MVP)

3. **(guest) layout** at `src/app/(guest)/layout.tsx`:
   - Server Component
   - Imports and renders `<GuestNav />` above `{children}`
   - Wraps children in `<main className="max-w-7xl mx-auto px-4 py-8">`
   - This layout applies to all routes inside the (guest) group

4. **Placeholder properties page** at `src/app/(guest)/properties/page.tsx`:
   - Simple Server Component that renders `<h1>Browse Villas</h1>` as placeholder
   - This confirms the (guest) layout renders correctly
   - Will be replaced in Plan 02 with the real browse page

IMPORTANT: The (guest) route group means the URL path is just `/properties`, NOT `/guest/properties`. Route groups with parentheses are layout-only — they do not affect the URL.

IMPORTANT: Do NOT move or touch existing routes in `src/app/(auth)/` or `src/app/(owner)/`. The guest route group is additive.
  </action>
  <verify>
    <automated>cd /Users/adamblackman/Code/whole-tel && npx next build 2>&1 | tail -20</automated>
  </verify>
  <done>Brand CSS variables are defined in globals.css and usable as Tailwind utilities (bg-brand-teal, text-brand-amber, etc.). GuestNav renders logo and nav links. The (guest)/layout.tsx wraps the properties page with GuestNav. The placeholder /properties page renders with the guest layout. Build succeeds with no errors.</done>
</task>

</tasks>

<verification>
1. `npm ls yet-another-react-lightbox` shows the package installed
2. `npm ls react-day-picker` shows the package installed
3. `src/components/ui/calendar.tsx` exists
4. `src/app/globals.css` contains `--brand-teal` and `--brand-amber`
5. `src/components/GuestNav.tsx` exists and exports a component
6. `src/app/(guest)/layout.tsx` exists and imports GuestNav
7. `npx next build` succeeds
</verification>

<success_criteria>
- All Phase 4 dependencies are installed (yet-another-react-lightbox, react-day-picker, shadcn Calendar)
- Brand color tokens are available as Tailwind utilities
- Guest-facing navigation and layout are ready for browse and listing pages
- No existing routes or components are broken
</success_criteria>

<output>
After completion, create `.planning/phases/04-guest-browsing/04-01-SUMMARY.md`
</output>
