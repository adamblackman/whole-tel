---
phase: 15-amenities-owner-ui-guest-display
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - src/lib/actions/amenities.ts
  - src/components/dashboard/AmenitiesEditor.tsx
  - src/app/(owner)/dashboard/properties/[propertyId]/edit/page.tsx
  - src/components/dashboard/PropertyForm.tsx
autonomous: true
requirements: [AMEN-02]

must_haves:
  truths:
    - "Owner sees a checkbox grid of ~30 amenities grouped into 5 categories on the property edit page"
    - "Owner can check/uncheck amenities and save with a dedicated 'Save Amenities' button"
    - "Saved amenity selections persist across page reloads"
    - "New property creation form does NOT show AmenitiesEditor (no propertyId yet)"
  artifacts:
    - path: "src/lib/actions/amenities.ts"
      provides: "upsertPropertyAmenities Server Action"
      exports: ["upsertPropertyAmenities"]
    - path: "src/components/dashboard/AmenitiesEditor.tsx"
      provides: "Client Component with checkbox grid by category"
      min_lines: 50
    - path: "src/app/(owner)/dashboard/properties/[propertyId]/edit/page.tsx"
      provides: "Edit page with catalog+selection fetch and AmenitiesEditor mount"
  key_links:
    - from: "src/components/dashboard/AmenitiesEditor.tsx"
      to: "src/lib/actions/amenities.ts"
      via: "useTransition + Server Action call"
      pattern: "upsertPropertyAmenities"
    - from: "src/app/(owner)/dashboard/properties/[propertyId]/edit/page.tsx"
      to: "src/components/dashboard/AmenitiesEditor.tsx"
      via: "Server-fetched catalog and selectedIds passed as props"
      pattern: "AmenitiesEditor.*catalog.*selectedIds"
---

<objective>
Owner amenity management via structured checkbox grid on the property edit page.

Purpose: Replace the hardcoded 12-option freetext AMENITY_OPTIONS in PropertyForm with a database-driven AmenitiesEditor that reads from the amenities catalog table (30 items, 5 categories) and writes to the property_amenities join table via a dedicated Server Action.

Output: Working owner amenity selection UI that saves to the join table independently from the main property form.
</objective>

<execution_context>
@/Users/adamblackman/.claude/get-shit-done/workflows/execute-plan.md
@/Users/adamblackman/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/15-amenities-owner-ui-guest-display/15-RESEARCH.md

<interfaces>
<!-- Key types and contracts the executor needs. Extracted from codebase. -->

From src/lib/dal.ts:
```typescript
export async function requireOwner(): Promise<{ id: string; email: string; role: string }>
```

From src/lib/supabase/server.ts:
```typescript
export async function createClient(): Promise<SupabaseClient>
```

From src/components/dashboard/PropertyForm.tsx (lines 12-25 — to be cleaned up):
```typescript
const AMENITY_OPTIONS = ['Pool', 'Hot Tub', 'BBQ', 'Ocean View', 'Rooftop', 'Private Beach', 'Chef Kitchen', 'Game Room', 'Home Theater', 'Gym', 'Parking', 'WiFi'] as const

interface PropertyFormProps {
  initialData?: {
    amenities?: string[]  // This field becomes unused — AmenitiesEditor is separate
    // ... other fields unchanged
  }
}
```

From existing edit page (src/app/(owner)/dashboard/properties/[propertyId]/edit/page.tsx):
```typescript
// Current query selects: 'id, name, description, location, address, bedrooms, bathrooms, max_guests, nightly_rate, cleaning_fee, amenities, house_rules, check_in_time, check_out_time, bed_config, guest_threshold, per_person_rate, property_photos(...)'
// Needs: add parallel query for amenities catalog + property_amenities selectedIds
```

Amenities DB schema (from Phase 12 migration):
- `amenities` table: id (uuid), name (text UNIQUE), category (text), icon_name (text), display_order (int)
- `property_amenities` table: property_id (uuid FK), amenity_id (uuid FK), PK(property_id, amenity_id)
- RLS on property_amenities: SELECT open to all, INSERT/UPDATE/DELETE requires owner_id match via properties table
- 5 categories: Water, Social, Work/Event, Culinary, Wellness
- 31 amenities seeded
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Server Action and shadcn components</name>
  <files>src/lib/actions/amenities.ts, (shadcn scaffold: src/components/ui/checkbox.tsx)</files>
  <action>
1. Run `npx shadcn add checkbox` to scaffold the Checkbox component (needed by AmenitiesEditor).

2. Create `src/lib/actions/amenities.ts` with a `upsertPropertyAmenities` Server Action:
   - Mark `'use server'` at top
   - Import `createClient` from `@/lib/supabase/server`, `requireOwner` from `@/lib/dal`, `revalidatePath` from `next/cache`
   - Signature: `export async function upsertPropertyAmenities(propertyId: string, amenityIds: string[]): Promise<{ error?: string }>`
   - Call `requireOwner()` for auth
   - Verify property ownership: query `properties` where `id = propertyId AND owner_id = user.id`, return error if not found
   - Delete all rows from `property_amenities` where `property_id = propertyId`
   - If `amenityIds.length > 0`, insert rows: `amenityIds.map(amenity_id => ({ property_id: propertyId, amenity_id }))`
   - `revalidatePath('/dashboard/properties/' + propertyId)` and `revalidatePath('/properties/' + propertyId)`
   - Return `{}` on success

Follow the exact pattern from `src/lib/actions/properties.ts` (requireOwner + ownership check + mutation + revalidate).
  </action>
  <verify>
    <automated>cd /Users/adamblackman/Code/whole-tel && npx tsc --noEmit 2>&1 | head -20</automated>
  </verify>
  <done>Server Action compiles, checkbox component scaffolded</done>
</task>

<task type="auto">
  <name>Task 2: AmenitiesEditor component and edit page integration</name>
  <files>src/components/dashboard/AmenitiesEditor.tsx, src/app/(owner)/dashboard/properties/[propertyId]/edit/page.tsx, src/components/dashboard/PropertyForm.tsx</files>
  <action>
1. Create `src/components/dashboard/AmenitiesEditor.tsx` as a `'use client'` component:
   - Props interface: `{ catalog: AmenityCatalogRow[], selectedIds: string[], propertyId: string }`
   - Where `AmenityCatalogRow = { id: string; name: string; category: string; icon_name: string; display_order: number }`
   - Use `useState<Set<string>>` initialized from `selectedIds` prop
   - Use `useTransition` for save pending state
   - Define `CATEGORIES = ['Water', 'Social', 'Work/Event', 'Culinary', 'Wellness'] as const`
   - Render each category as a section with heading (`text-sm font-medium text-muted-foreground mb-2`)
   - Within each category, render a grid (`grid grid-cols-2 sm:grid-cols-3 gap-2`) of checkboxes using shadcn `Checkbox` component
   - Each checkbox label shows amenity name
   - Toggle function: copy Set, add/delete, setState
   - "Save Amenities" button at bottom using shadcn `Button` — calls `upsertPropertyAmenities(propertyId, [...selected])` inside `startTransition`
   - Show success state: after save resolves without error, briefly show "Saved!" text (use a `useState<boolean>` that auto-resets via `setTimeout` after 2 seconds)
   - Show error state: if result has `.error`, display it in red text
   - Wrap entire component in a section with heading "Amenities" (`text-lg font-semibold`) and a `Separator` above it

2. Update edit page (`src/app/(owner)/dashboard/properties/[propertyId]/edit/page.tsx`):
   - Add two parallel Supabase queries alongside the existing property query:
     - `supabase.from('amenities').select('id, name, category, icon_name, display_order').order('display_order')`
     - `supabase.from('property_amenities').select('amenity_id').eq('property_id', propertyId)`
   - Extract `catalog = catalogResult.data ?? []` and `selectedIds = (selectedResult.data ?? []).map(r => r.amenity_id)`
   - Render `<AmenitiesEditor catalog={catalog} selectedIds={selectedIds} propertyId={propertyId} />` AFTER the PropertyForm/PhotoManager block, separated by a `<Separator />`
   - Do NOT place AmenitiesEditor inside PropertyForm — it saves independently

3. Clean up PropertyForm.tsx:
   - Remove the `AMENITY_OPTIONS` constant (lines 12-25)
   - Remove the amenities checkbox section from the form JSX (search for "Amenities" heading and the checkbox grid that maps over AMENITY_OPTIONS)
   - Keep `amenities?: string[]` in the `initialData` interface for now (avoids breaking the `new` page which still passes it) — it just won't be rendered
   - Do NOT remove the `amenities` field from the form submission handling in the Server Action (properties.ts) — that's a separate concern and the JSONB column still exists

Note: The `/dashboard/properties/new` page does NOT get AmenitiesEditor — owners must save the property first, then edit to add amenities. This is noted in the research as the correct approach (join table writes require a property ID).
  </action>
  <verify>
    <automated>cd /Users/adamblackman/Code/whole-tel && npx tsc --noEmit 2>&1 | head -20</automated>
  </verify>
  <done>AmenitiesEditor renders on edit page with checkbox grid by category; Save Amenities button triggers upsert; PropertyForm no longer shows old freetext amenity checkboxes</done>
</task>

</tasks>

<verification>
- `npx tsc --noEmit` passes with zero errors
- `npx next build` succeeds
- Manual: Visit /dashboard/properties/{id}/edit — see amenity checkbox grid grouped by 5 categories
- Manual: Check several amenities, click "Save Amenities", reload page — selections persist
- Manual: Visit /dashboard/properties/new — no amenity checkboxes shown (only on edit)
</verification>

<success_criteria>
Owner can select/deselect amenities from the full 31-amenity catalog via checkbox grid on the property edit page. Selections save to the property_amenities join table via a dedicated Server Action and persist across page reloads. The old hardcoded AMENITY_OPTIONS freetext checkboxes are removed from PropertyForm.
</success_criteria>

<output>
After completion, create `.planning/phases/15-amenities-owner-ui-guest-display/15-01-SUMMARY.md`
</output>
